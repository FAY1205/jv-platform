// Shared Survey map styling (WP-D; hex CoverageMap retired D1 2026-07-15 —
// CountyCoverageMap is now the sole consumer, constants kept here as the one
// styling home should another map surface appear).

/** Covered-territory fill opacity — the survey-paper softening (DIRECTION §Signature). */
export const PARTNER_FILL_OPACITY = 0.9;

/** Explore-mode dim level for non-selected territory ("highlight a partner"). */
export const DIMMED_FILL_OPACITY = 0.28;
