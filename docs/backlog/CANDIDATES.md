# WP candidates — noticed in passing, not built

Small items surfaced during WP work (PLAYBOOK §6 "deferred improvements" line). Each is
a stub until promoted to a real WP-NNN file. Delete a line when promoted or rejected.
Big deferred features live in the CRM capability map (WP-TSK-SERIES / memory), not here.

Numbers are allocated per review, never reused.

| # | Candidate | Source WP | Notes |
|---|-----------|-----------|-------|
| C-1 | ✅ **DONE** (`claude/hygiene-batch`): eslint `globalIgnores` + tsconfig `exclude` now skip `laravel-crm-2.2/` + `_marketing/`; local typecheck is clean. Exclude `laravel-crm-2.2/`, `demo-app.html`, `_marketing/` from tsconfig + eslint | WP-TSK-1 | Untracked reference folders pollute local `typecheck` (3+ TS errors) and `lint` (63 errors); CI unaffected. One-line ignore each. |
| C-2 | ✅ **PROMOTED → WP-SEC-2** (`claude/wp-sec-2`, migration 0044). **WP-RLS-PARITY** — bring every lead-child policy up to 0041's two-half standard | WP-TSK-1 (audit-tenancy F-1/F-6, HIGH priority) | Verified live: `anon`/`authenticated` hold full CRUD grants on `leads`/`lead_notes`/`lead_tasks`, so RLS is the only PostgREST gate. (a) `lead_notes_scope` (0010) lacks own-org-author, subquery tenant filter, and `deleted_at` — the R-22 leak 0037 fixed for status history exists for notes at the DB layer; (b) ALL sibling policies (`leads`, `lead_notes`, `lead_status_history`, `listing_checks`) carry tenant-only WITH CHECK — a partner JWT can write cross-stream/cross-ownership via PostgREST; 0041 shows the target shape. One migration (0037 as template; journal-`when` bump). Also adopt the audit's proposed spec rules: WITH CHECK ≥ USING on tenant/stream/ownership; composite-FK-or-scoped-parent-lookup for child tables; new free-text lead-child columns join void/retention redaction in the WP that makes them writable; revert-leg tests for overlay-dependent predicates. Plus a repo-wide `pg_policies.with_check` probe in isolation.test.ts. |
| C-3 | ✅ **DONE → WP-AUTH-OUTAGE** (`claude/wp-auth-outage`, migration 0048, **SEC-09**). (a) **503 + Retry-After** — new `jsonServiceUnavailable` helper (lib/http.ts; logs w/ shared traceId, `Retry-After`, no-store); login's infra-outage path (`success===undefined`) returns a floored **503** not a 500 (retryable, AUT-05-safe since account-independent). New spec ID **SEC-09** minted (SEC-08 was already the shipped-headers ID — pr-reviewer F-1). Folded in the sibling `change-password` infra branch (pr-reviewer F-2): was a bare 503 with no log/Retry-After → now the same helper + throw capture. (b) **getSupabaseServer outside floor** — already satisfied in WP-SU-20: login + change-password construct the client *before* `withUniformTiming`; `otp/verify` DOES reach it (via `establishSessionForEmail`) but INSIDE its floor by design (session-establish is part of the timed outcome) — no change. (c) **SU-18 index rename** — `notice_claims_identifier_kind_key`→`_idx` (a `CREATE UNIQUE INDEX` takes `_idx`) via migration 0048 `ALTER INDEX RENAME` (metadata-only, no drop-window) + schema.ts; code refs the columns via ON CONFLICT, not the name. Tier A (prod migration → owner greenlight). | WP-SU series | Carried over from the SU-16..20 line. |
| C-4 | ✅ **DONE — decision: scope-gate, not role-gate** (`claude/candidates-batch-1`, ADR-0047). `/api/portal/*` stays scope-gated (PRN-08) + ToS-gated with NO `requirePartnerResponse`: an admin scope degrades to an admin-scoped, tenant-bounded read (`visibleLeadsWhere`; the distribution hold already keys on `role==='partner'`), which is intentional defense-in-depth and keeps admin-preview-portal open. A partner-only guard was rejected (would break portal-scope tests + foreclose admin-preview). Audience now explicit. `/dashboard` getServerScope dedupe NOT done (separate, deferred). Role-gate `/api/portal/*`; dedupe `/dashboard` getServerScope | Portal parity | Carried from testing round 3 open items. WP-TSK-3 (audit-tenancy F-9) adds: declare the audience of `/api/portal/*` EXPLICITLY — either a `requirePartnerResponse` guard mirroring `requireAdminResponse`, or a recorded decision that admin-readable is intentional (an admin scope currently flows through `getPartnerLeadDetail` unharmed, and portal-scope tests rely on that). Silence is the problem, not the current behaviour. |
| C-5 | ✅ **PARTLY DONE** (`claude/hygiene-batch`): `LeadNotFoundError` consolidated into `src/modules/leads/errors.ts`, re-exported from all 5 modules (identity preserved so route `instanceof` catches still match; refId optional to cover the commands.ts no-arg throw). **`partnerLive` NOT done — deferred to C-8** (WP-TSK-2a folds the hold into `taskWhere`/`noteWhere` + RLS, which eliminates the two `partnerLive` copies rather than deduping a compensator that's about to be removed). Extract the duplicated `LeadNotFoundError` + `partnerLive(scope)` helper | WP-TSK-2 | Four modules now declare their own identical `LeadNotFoundError` (notes, tasks, leads/commands, portal/status-update) and two copy `partnerLive` verbatim (notes.ts, tasks.ts). One shared `modules/leads/errors.ts` + a scope-level helper would keep the hold/soft-delete rule in ONE place — today a change to partner lead reachability must be made twice. Deliberately duplicated in WP-TSK-2 to keep the notes/tasks diff readable; worth collapsing before a third stream lands. |
| C-6 | ✅ **DONE — decision: keep fixed** (`claude/candidates-batch-1`). My Tasks page size stays fixed at 20 — a personal work list, not a data table; no UI exposes a selector (WP-TSK-5). Affirmed in the `MY_TASKS_PAGE_SIZE` comment with the convention to adopt shared `pageSizeParam()` IF a selector is ever built (so no second convention is invented). No rate-limit added — consistent with the other list endpoints. `lead_tasks` list endpoints have no rate limit / no `pageSize` control | WP-TSK-2 | `GET /api/tasks` uses a fixed page size (20) and the shared `pageParam` cap; if the My Tasks UI (WP-TSK-5) wants a size selector, adopt `pageSizeParam()` like the activity/leads endpoints rather than inventing a second convention. |
| C-7 | ✅ **DONE** (`claude/candidates-batch-1`). `/api/cron/retention-sweep` now sums and returns `notesRedacted` + `tasksRedacted` alongside `purged` (the per-artifact counts `sweepTenantPii` already computes + audits per lead), so LGL-02 evidence has per-artifact figures without reading `audit_log`. Retention sweep audit payload gained `tasksRedacted` — consider a compliance-report surface | WP-TSK-2 | `sweepTenantPii` now returns and audits `tasksRedacted` alongside `notesRedacted`, but the cron route still reports only `purged`. If LGL-02 evidence ever needs per-artifact counts, thread both through `/api/cron/retention-sweep`'s response. |
| C-8 | ✅ **DONE → WP-TSK-2a** (`claude/wp-tsk-2a`, migration 0047). Folded `releasedLeads(now)` into `taskWhere`/`noteWhere` partner `ownLeads` subqueries + the partner arms of `lead_tasks_scope`/`lead_notes_scope` (USING + WITH CHECK, WITH CHECK ≥ USING) in migration 0047 (journal `when` bumped). Deleted `partnerHoldGate` + its 4 call sites; `taskVisibleTo` threads the reminder clock through `taskWhere(scope, db, now)`. Kept `partnerLive` (write-path lead-resolution gate) + `liveLeadGate`. RLS oracle proves it: new RLSB-04 (C-8) held-lead read+write denial; RLP/RLSB/tasks-scope seeds backdated (`RELEASED_AT`) so partner-visible leads are released. C-15 (`role='partner'` in `ownAuthors`) already shipped in 0044 — verified. **WP-TSK-2a** — fold the distribution hold into `taskWhere`'s partner arm, RLS in lockstep | WP-TSK-2 (audit-tenancy F-3) | `taskWhere` filters partner ownership + soft-deletes but is hold-BLIND, so `resolveTask` and `listMyTasks` carry local `partnerHoldGate(...)` conjuncts (src/modules/tasks/tasks.ts) to close it. That is the guard's job, not the module's: move `releasedLeads()` into `taskWhere`'s `ownLeads` subquery, move the same predicate into `lead_tasks_scope`'s partner arm in the SAME migration (journal-`when` bump), then delete the local conjuncts. WP-TSK-2 could not do it — the brief forbade touching `lib/scope.ts` and migration 0041. Note `noteWhere` has the identical gap, compensated the same way in `notes.ts`; fix both arms together. |
| C-9 | Portal timeline's "Lead received" anchor uses `firstMatchedAt` — owner call needed | WP-TSK-3 (audit-tenancy F-10) | `getPartnerLeadDetail`'s TSK-06 anchor (and the long-standing `receivedAt` field it reuses) is the lead's FIRST routing timestamp. On a re-routed lead that predates the current owner's tenure, so their timeline opens with a date from before they had the lead — harmless as information (no prior-partner identity is exposed), but arguably wrong as a fact. Options: keep as-is (consistent with `receivedAt` everywhere), or use `coalesce(manual_assigned_at, first_matched_at, created_at)` so each owner sees their own start. Owner decision; changing it touches `receivedAt` semantics across the portal, not just the timeline. |
| C-10 | ✅ **DONE — Phase C** (`src/lib/use-current-user.ts` + `/api/me` capabilities, PR #118). Client-side current-user id/role hook | WP-TSK-4 (pr-reviewer F-4) | `TasksPanel` can't tell which task the acting user authored — the payload (`LeadTaskView`) carries `authorUserId` but the client never learns its OWN user id/role, so it cannot hide the (doomed-to-404) Delete affordance for a task the viewer didn't author, or otherwise vary UI by authorship/stream. Needs a small `useCurrentUser()`-style hook (or a scope field threaded onto the lead-detail/tasks payload) before any authorship-aware UI can be built. |
| C-11 | ✅ **DONE 2026-08-18** (`claude/slice3-c11-c12-assignee`). `LeadTaskView` grew `assignee`/`author` `{email, role, deactivated}\|null` via tenant+stream-scoped LEFT JOINs (`identityJoin`/`sameStreamUsers` — the write path's own predicate, so read and write can't drift); TasksPanel renders ONE identity per row (`assignee ?? author`, 20px `AvatarInitials xs` + name, "You" via `useCurrentUser` email equality, role tooltip-only); Delete hidden for non-authors, read-only tiers get disable+tooltip (`canWrite` prop on the portal — partners carry no capability array). Task assignee avatar + name display | WP-TSK-4 (design F-5) | The mockup shows an assignee avatar + first name per task; the current `LeadTaskView` payload carries only `assignedToUserId`/`authorUserId` (raw UUIDs, no email/name), so `TasksPanel` omits assignee display entirely rather than inventing it (v1 also has no assignee picker — self-assign only, TSK-03). Revisit once the payload can resolve a display name, and once C-10 exists so the row can special-case "You". |
| C-12 | ✅ **DONE 2026-08-18** (same branch as C-11). Panel + `history` field removed from the portal lead dialog and `PartnerLeadDetail` (the scoped `hist` query stays — it feeds `activity` + the derived status); `portal-scope.test.ts` PTL-03/R-22 legs migrated onto `activity.filter(kind==="status")` with identical isolation semantics; Timeline's Status filter gained the old panel's empty-state line. Retire the portal's legacy "Status history" panel | WP-TSK-4 (pr-reviewer F-6 / design F-6) | `PortalLeadDialog` now renders both the new `Timeline` (whose Status filter shows the same status-change entries) and the pre-existing standalone "Status history" list (PTL-02/03) — kept side-by-side deliberately in WP-TSK-4 rather than removed, since retiring an existing feature felt outside that WP's explicit scope. Once the Timeline's Status filter is confirmed equivalent in practice, drop the separate panel and its `history` field from the client type. |

| C-13 | ✅ **DONE — WP-RET-3a + WP-RET-3b** (3a: `claude/wp-ret-3` #92 merged+prod-verified; 3b: `claude/wp-ret-3b`). **3b:** `sweepSavedViewsPii` clears the seller-PII search string `filters.q`→"" on saved_views untouched >12mo (keeps the view + structured filters; only `q` is free-text PII; idempotent, updated_at not bumped) + wired into the retention cron; **erasure runbook** at `docs/ops/erasure-runbook.md` (all 7 PII locations + the two purge triggers + subject-request steps). No migration. **3a (below):** ⏳ **PARTLY DONE — WP-RET-3a** (`claude/wp-ret-3`, migration 0049; notifications half). notifications gained `lead_ref` (0049) + a 90-day age sweep (`sweepNotifications`); `redact-lead-comms.ts` redacts a soft-deleted lead's notifications (by lead_ref) + email_outbox rows (by meta.leadRef) — called by BOTH `voidUpload` (immediate) and `sweepTenantPii` (backstop); `SweepResult`+cron gained `notificationsRedacted`/`outboxRedacted`. Owner chose KEEP task title (visibility) + redact-on-void, not generic titles. **STILL PENDING → WP-RET-3b:** `saved_views.filters.q` seller-PII sink (updated_at `q`-clear sweep) + the erasure runbook doc (all PII locations). **WP-RET-3** — notifications have NO retention sweep, and notification/outbox rows escape the lead PII purge | WP-TSK-6 (audit-tenancy F-2, Medium) | `run/void.ts` and `retention/sweep.ts` redact `leads`, `lead_notes.body` and `lead_tasks.title`, but the `notifications` table is touched by neither — and `email_outbox` rows keep their rendered subject/body/html indefinitely. WP-TSK-6 widens the exposure: a `task_due` notification title embeds the task title verbatim, so a void that sentinels the source row leaves a readable copy in the bell. Two halves: (a) give `notifications` a retention sweep like every other operational table; (b) extend the void/purge paths to redact or delete notification + outbox rows for the affected leads — they are correlatable via the outbox's `meta.leadRef` stamp and would need an equivalent marker on notifications. Also consider making in-app titles generic ("A task is due") with the specifics behind the deep link, which removes the copy problem at the source. Pre-existing for `status_change`; this WP made it worth a number. **WP-SV-1 (audit-tenancy F-2) adds a third surface:** `saved_views.filters.q` is a seller-PII sink — the leads search box takes seller names, phone fragments and addresses, and a saved view stores whatever was typed there verbatim, per user, forever (no sweep, and the lead purge cannot correlate it: the blob holds a search STRING, not a lead id). Two halves again: (a) a retention sweep keyed on `updated_at` for views untouched beyond the window, matching how the other operational tables age out; (b) list `saved_views` in the erasure runbook, since a subject-access or deletion request over a seller must reach text an admin typed about them. Cheap while the table is new — the fix is a sweep entry, not a schema change. |
| C-14 | ✅ **DONE — WP-TSK-6a** (`claude/wp-tsk-6a`, migration 0050). `lead_tasks += reminder_attempts` (0050, ADD COLUMN default 0 — no rewrite/lock). Sweep filters `reminder_attempts < REMINDER_ATTEMPTS_MAX` (=6, ~30min at 5-min ticks); the no-recipient path increments under the per-tenant advisory lock and the tick reaching MAX RETIRES the task (drops from candidates) + notifies admins ONCE (`task_reminder_orphaned`, generic + leadRef, no task-title PII). Wall-clock budget: `remindDueTasks` takes injected `deadlineMs`+`clockMs`; the loop stops claiming once real time passes it; drain-outbox cron sets one shared `runStart + 30s` deadline so no tenant starves the 60s job. Cron response += `tasksRetired`. **WP-TSK-6a** — terminal state for permanently-unresolvable reminders + a sweep time budget | WP-TSK-6 (audit-tenancy F-3) | `remindDueTasks` deliberately does NOT stamp a task whose recipient cannot be resolved, so a task orphaned by a re-route is re-probed on every cron tick forever (two visibility queries each), and it can never be nudged again even if ownership returns. Meanwhile the sweep is the only unbounded duty in the drain-outbox cron: a tenant with ≥ the batch limit (200) of due tasks silently starves — it makes progress only for tasks whose recipients resolve, and the rest re-run every tick. Wants: an explicit terminal marker (or an attempt counter) so an orphan is retired after N ticks and surfaced to an admin, plus a wall-clock budget for the sweep so one tenant cannot consume the job's 60s `maxDuration`. Interim mitigation shipped in the review fixes: the drain now runs BEFORE the sweep, so mail delivery and the Sentry check-in are never behind it. |
| C-15 | ✅ **PROMOTED → WP-SEC-2** (`claude/wp-sec-2`, RLP-06/SCP-01, migration 0044 + scope.ts). **WP-SCP-1** — add `role = 'partner'` to the `ownAuthors` subquery in `taskWhere` AND `noteWhere` (+ matching RLS) | WP-TSK-6 (audit-tenancy F-8) | Both guards resolve "my org's authors" as `users WHERE tenant_id = ? AND partner_id = ?`, with no role predicate — and `users.partner_id` carries no role invariant (nothing stops an admin row from holding one; `resolveAssignee` already had to add the role check locally for exactly this reason, audit-tenancy F-4). So an admin user with a stray `partner_id` would have their admin-authored rows counted as that org's, crossing the PRN-13 wall in a READ path. Latent today (no code path sets `partner_id` on an admin), but WP-TSK-6 promoted `taskWhere` from a visibility filter to a **delivery authorization** — it now decides who receives email — which raises the cost of the gap. Fix both builders and both RLS policies in one migration; pairs naturally with C-2 and C-8. |

| C-16 | **WP-KAN-1a** — LATERAL rewrite of the two correlated status subqueries + a covering index + push the column filter into `base` | WP-KAN-1 (audit-tenancy F-3 + pr-reviewer F-2) | `latestStatus`/`latestAt` (src/modules/leads/queries.ts) are per-row correlated subqueries evaluated once per lead, for BOTH the list and the board. The board makes it structural: `listLeadsBoard` builds `base` over the whole scoped, kept lead set and only slices with `row_number()` afterwards, so **cost scales with tenant size, not page size** — and a single-column "Load more" (`?status=X&page=2`, 25 rows) still pays a full-board scan because the status filter is applied after the window. Three parts, one WP: (a) replace both subqueries with one `LEFT JOIN LATERAL (select status, created_at from lead_status_history where lead_id = leads.id and <tenant> order by created_at desc, id desc limit 1) h on true` so the history is probed once per lead instead of twice; (b) add the covering index `lead_status_history (lead_id, created_at desc)` — today only `lead_status_lead_idx (lead_id)` exists, so every probe sorts; (c) push `col_status in (…)` down into `base` for the single-column path. **Alias trap:** the scope-aware fragments (`tenantWhere(schema.leads, …)`, `statusExpr`, every `${schema.leads.x}` ref) render the drizzle TABLE NAME, never an alias — which is why the CTE deliberately does `from leads` unaliased. A LATERAL rewrite that aliases `leads` (`from leads l`) will emit `"leads"."id"` inside a query where only `l` is in scope: it fails loudly on the board, but a partially-aliased WHERE could silently widen scope. Alias nothing, or convert every embedded fragment in the same pass. Measure first — the board is fast at current tenant sizes; this is a scaling item, not a bug. **Also fold in the portal's parallel copy:** `src/modules/portal/queries.ts` declares its OWN `latestStatus`/`statusExpr`, which did NOT get the `, id desc` tie-break the admin pair received in the WP-KAN-1 review fixes (portal code was out of that WP's scope). Until it does, a same-timestamp tie can read one way in the admin list/board and another in the partner portal. Same probe, same index, same tie-break — do all three call sites together. |
| C-17 | ✅ **DONE** (`claude/hygiene-batch`): `/api/leads` (list) and `/api/leads/[ref]` (detail) 500 paths moved onto `jsonServerError` (static message + logged traceId); `leads-route-errors.test.ts` proves the body no longer carries the driver text (seller phone / SQL). `jsonServerError` sweep across the two sibling leads GETs | WP-KAN-1 (pr-reviewer F-4) | `/api/leads` (list) and `/api/leads/[ref]` (detail) still answer their 500 path with `jsonError(code, e.message, 500)`: the raw error text — which for a driver error includes the failing SQL's bound params, i.e. seller data — is echoed to the client, and nothing is logged, so the returned traceId correlates to no server line. `GET /api/leads/board` was moved onto `jsonServerError` (static message + logged traceId) in the WP-KAN-1 review fixes; the two siblings share the pattern and should follow. Mechanical, but it touches the leads list contract, so it wants its own diff + a route test asserting the body no longer carries the driver text. |
| C-18 | ✅ **DONE — decision: KEEP** (`claude/cleanup-batch-2`, owner decided "yes"). The `listLeads` + `listLeadsBoard` display joins deliberately do NOT filter `deleted_at` — a soft-deleted partner keeps its name/colour on the historical leads it handled (PRN-05 attribution; blanking to "Unmatched" is a different lie). `unmatchedCoverageMatches` still excludes deleted/revoked partners because it ROUTES new work. Decision recorded as comments at both join sites; no behaviour change. Partners-join soft-delete filter (`deleted_at is null`) in `listLeads` + `listLeadsBoard` together | WP-KAN-1 (pr-reviewer F-5) | Both the list's `leftJoin(partners, …)` and the board's CTE join match on id + tenant only, so a SOFT-DELETED partner still lends its name/colour to every lead it owns. Elsewhere (`unmatchedCoverageMatches`) the same lookup filters `deleted_at is null` + `status <> 'revoked'` and DROPS the match. Which is right is a product question — a deleted partner's historical leads arguably should keep showing who handled them (PRN-05 spirit), and blanking them would make those leads read "Unmatched", which is a different lie. Decide once, apply to both call sites in one diff, and record the answer as a comment where the join lives. |
| C-19 | **WP-LGL-2** — ToS-gate sweep across admin data routes, or an ADR recording page-gate-only | WP-KAN-1 (audit-tenancy F-8, pre-existing class) | `requireTosResponse` guards the portal's routes; admin data routes (including the new `/api/leads/board`) are gated only at the page level, so an authenticated admin who has not accepted the current ToS can still read data through the API directly. Pre-existing for every admin route — the board just joined the class. Either apply the guard to admin data routes too, or write the ADR that says page-level acceptance is the intended boundary and why. Pairs with the WP-LGL-1 publishing work (owner-blocked). |
| C-20 | ✅ **DONE** (`claude/cleanup-batch-2`): SPEC.md §6.23 Tags (TAG-01..07), §6.24 Global search (SRCH-01..05), §6.25 Saved views (SV-01..05) added, plus **DM-12 (bounded params)** in §5. Fold the shipped slice-3 requirement IDs (SRCH-01..05, TAG-01..07, SV-01..05) into `docs/SPEC.md` as numbered sections | WP-SRCH-1 (audit-tenancy F-2) | The series brief `docs/backlog/WP-S3-SERIES.md` is the only place these IDs are defined, and CLAUDE.md tells every session to "read docs/SPEC.md section(s) named in the current work package" — so a future session auditing SRCH/TAG/SV finds requirement IDs with no spec home. Backlog briefs are the right shape for planning, not for the durable contract. Do it once the whole slice (TAG-1 + SRCH-1 + SV-1) has landed, so the sections describe what actually shipped rather than what was proposed. **All three have now landed (WP-SV-1 closes the slice), so this is unblocked** — and WP-SV-1 (audit-tenancy F-7) adds two things the backfill should carry: the SV requirement text now has decisions the brief never stated (per-user visibility → ADR-0045; sort/dir deliberately outside the filters blob; no live counts), and the bounded-params rule below. **Proposed spec rule DM-12 (bounded params):** *every query parameter that is split, expanded or turned into an IN-list is bounded BEFORE validation, not only after it — the bound applies to the parse (segment count) as well as to the result (allow-list or cap), because an unbounded split materialises the attacker's string before any filter runs.* Implemented three times already (`tagsParam` TAG_PARAM_MAX_SEGMENTS, `csv` CSV_MAX_SEGMENTS, `pageParam`'s clamp); it wants to be a numbered rule so the fourth param author inherits it instead of rediscovering it. |
| C-21 | ✅ **DONE — WP-HDR-1** (`claude/candidates-batch-1`). `jsonOk` now sets `Cache-Control: private, no-store` on every success payload (one line, reaches every route) — independent of the front proxy's page-level `no-store` (`src/proxy.ts`), which only fires on a resolved Supabase session and so misses bearer/cron responses. Verified no endpoint serves a deliberately-cacheable jsonOk body (every explicit `Cache-Control` in the tree was already `no-store`); file downloads set their own headers via `new Response`. `http.test.ts` pins it. **WP-HDR-1** — `Cache-Control: private, no-store` on authenticated API responses | WP-SRCH-1 (audit-tenancy F-10) | `jsonOk` sets no cache headers, so every authenticated JSON response (leads, search, dashboard, portal) relies on the default heuristics of whatever sits in front of it. Tenant-scoped payloads must never be cacheable by a shared proxy. One line in `lib/http.ts` reaches every route at once — which is also why it wants its own diff: a blanket header change can break a deliberately-cacheable endpoint (check the cron/health routes first). Pairs naturally with a headers audit of `security-headers.ts`. |
| C-22 | Global search: wrap the lead row query in a CTE so `statusExpr` runs per RETURNED row | WP-SRCH-1 (audit-tenancy F-5) | `globalSearch` selects the correlated latest-status subquery alongside the row projection with `limit 10`; the planner is free to evaluate that subplan for every MATCHED row rather than the ten returned, so a query matching thousands of leads pays thousands of history probes. A `with hits as (select id … order by created_at desc limit 10)` then joining the status expression onto those ten would bound it. **Measure first** — `EXPLAIN ANALYZE` on a realistic tenant, since the planner may already push the limit down; this is a scaling item, not a bug. Same family as C-16 (the LATERAL rewrite), and best done in that WP so all four call sites get one treatment. |

| C-23 | ✅ **PROMOTED → WP-SEC-1** (`claude/wp-sec-1`, `tests/helpers/rls.ts` + `rls-behaviour.test.ts`, RLSB-01..05). Learned mid-build: a RETURNING-based write check is UNSOUND (RETURNING re-applies the USING/SELECT policy to the new row, so a write a weak WITH CHECK lets persist still 42501s on RETURNING → false denial). The oracle measures ACTUAL persistence via `reset role` + owner-side count. Also: for UPDATE on a FOR ALL policy the new row is checked against USING **and** WITH CHECK; for INSERT only WITH CHECK — so the pre-0044 holes were INSERT-only (fabrication / cross-stream / onto-unowned), while UPDATE re-route was already blocked. **WP-TEN-RLS-BEHAVIOUR** — execute the RLS policies as a non-owner role, not just assert their TEXT | WP-TAG-1 (audit-tenancy F-4) | Every RLS assertion in the suite today (`isolation.test.ts`, `tasks-scope.test.ts`, `tags-scope.test.ts`) reads `pg_policies.qual` / `.with_check` and pattern-matches the predicate — counted `app_current_tenant()` occurrences, `toContain("author_role")`, and so on. That is necessary but **not sufficient**: it proves the policy SAYS the right thing, never that Postgres ENFORCES it. A policy attached to the wrong table, a missing `ENABLE ROW LEVEL SECURITY` (only `relrowsecurity` is separately checked), a `FORCE`-less owner bypass, or an `app_current_*()` helper that silently returns NULL for an unauthenticated JWT would all pass the text assertions. Wanted: one shared helper (`tests/helpers/rls.ts`) that opens a transaction, `set local role authenticated` + `set local request.jwt.claims` to a given tenant/role/user, runs a read and a write, and asserts denial — then use it from BOTH 0041's and 0042's suites (and retro-fit `isolation.test.ts`). Note the app connects as the table owner and bypasses RLS by design (ADR-0013), so this is deliberately a NON-app connection path — it tests the PostgREST/anon surface, which is the only reason these policies exist. Pairs with C-2 (WP-RLS-PARITY): that WP rewrites the policies, this one gives it a real oracle. |
| C-24 | ✅ **DONE — PR #126 (2026-08-18).** Cap-not-virtualize: `TAG_LIMIT=100`/tenant (half the FEP-03 ~200 threshold → the plain picker list is compliant by construction, no new dep) enforced in `createTag` under a per-tenant `pg_advisory_xact_lock` (suffix key `tenantId+":tags"`; also made the round-robin color exact) with 409 `tag_limit_reached`; `GET /api/tags` → bounded `{tags,total,limit}` (ORDER lower(name),id + LIMIT); TagPicker keyboard `scrollIntoView` fix + count line ≥25 + at-cap hint; Settings "N of 100" + disabled create at cap + usage-count links to `/leads?tags=`; `TAG_PALETTE` append-only pinned in tokens.test.ts (TAG-08/TAG-09 minted; audit-tenancy PASS incl. cross-tenant `total` leg). Tag roster has no upper bound; picker is unvirtualized | WP-TAG-1 (pr-reviewer F-4) | `GET /api/tags` returns EVERY tag in the tenant with no pagination, and `TagPicker` renders the filtered roster as a plain `<ul>` (no virtualization) inside a `max-h-56` scroller. Fine for the v1 shape — tags are hand-created by admins, so a tenant realistically has 5–30 — but the FRONTEND_STANDARDS ~200-row virtualization rule would bite if tag AUTOMATION ever lands (auto-tag rules are already recorded as out of scope in WP-S3-SERIES), since machine-created tags have no natural ceiling. Revisit together: cap/paginate the roster endpoint, virtualize the picker list, and consider a per-tenant tag limit at creation. **Fold in the palette-reorder note:** `TAG_PALETTE` (src/lib/tokens/tokens.ts) is append-only by contract — rows store the KEY, so re-ordering the array silently recolors every existing tag. If the palette ever needs re-ordering (or a key renaming), it needs a data migration, not an edit; the array's own comment says so, but there is no test pinning it. |
| C-25 | ✅ **DONE** (`claude/cleanup-batch-2`): swept all 8 `updatedAt: new Date()` sites (ai/credential ×2, ai/settings, notify/prefs, settings/export-settings, notes, tags, tasks) to ``sql`now()`` — one DB clock. `updatedAt: new Date()` two-clock sweep — prefer ``sql`now()``` on rows whose defaults come from the DB clock | WP-SV-1 (pr-reviewer F-4) | `created_at`/`updated_at` default to Postgres' `now()`, but several UPDATE paths stamp `updatedAt: new Date()` from the APP server's clock — `modules/tags/tags.ts` (`updateTag`) is the closest sibling, and the pattern is copied wherever a module touches a timestamped row. Mixing two clocks in one column is invisible until something ORDERS by it: WP-SV-1 hit exactly that, where a few seconds of skew between the test host and the Supabase instance sorted a just-re-saved view BELOW one created moments earlier (caught live by `saved-views-api.test.ts`, fixed by switching that write to ``sql`now()```). Nothing orders by `tags.updated_at` today, so this is prevention, not a bug: sweep the call sites, and consider a trigger or a shared `touch()` helper so the choice isn't per-author. |
| C-26 | ✅ **BUILT → WP-SEC-3** (`claude/wp-sec-3`, migration 0045 + SEC3-01..04, owner-approved). REVOKE table DML grants from `anon`/`authenticated` on the lead-family tables (least privilege; the real fix WITH CHECK cannot express) | WP-SEC-2 (audit-security F-1, **HIGH**) | WITH CHECK parity (0044) closes cross-owner/cross-stream INSERT/UPDATE, but **RLS cannot gate `DELETE` or column scope**. The app authenticates partners via Supabase Auth, so a real partner JWT (`role:authenticated`) reaches PostgREST and can `DELETE` its own leads/notes/tasks/status/listing rows (USING admits them; WITH CHECK doesn't apply to DELETE) — hard-deleting what the void model soft-deletes (PRN-05/DM-08) — or `UPDATE` non-ownership columns of owned leads (`rawJson`, `score_total`, `mls_status`). **Verified safe to revoke:** grep shows NO client-side `supabase.from()` access anywhere; the app queries these tables only via API routes on the owner connection. Fix: `revoke insert, update, delete on leads, lead_notes, lead_tasks, lead_status_history, listing_checks from anon, authenticated;` (+ consider SELECT if the portal doesn't read via PostgREST — it doesn't). Then RLS becomes pure defense-in-depth (its intended role, ADR-0013). Add ADR-0046 §Decision-6 + a DELETE-blocked behaviour test via the WP-SEC-1 oracle. **Owner decision: it's a prod grant change (migrate-on-merge), so greenlight before merge.** Pairs with C-8. |
| C-28 | ✅ **BUILT → WP-SEC-4** (`claude/wp-sec-4`, migration 0046 + SEC4-01..02, owner-approved). Schema-wide least privilege: `ALTER DEFAULT PRIVILEGES` + revoke DML from `anon`/`authenticated` on ALL tenant tables (not just lead-family) | WP-SEC-3 (pr-reviewer F-1, **HIGH**) | **Verified via `pg_default_acl`:** the `public` schema auto-grants FULL DML (`arwdDxtm`) to both `anon` and `authenticated` on every NEW table created by `postgres`. So WP-SEC-3's per-table revoke (0045) fixes only the 5 named tables — any FUTURE lead-family table (the `lead_tasks`/`listing_checks` pattern) OR any other tenant table (`tags`, `saved_views`, `partners`, `coverage_zips`, …) silently carries the same DELETE/column-tamper hole. The durable fix: `alter default privileges in schema public revoke insert, update, delete, truncate on tables from anon, authenticated;` + a one-time revoke sweep over all existing public tables, then a DERIVED guard test (every `*_scope` table has zero DML grant for anon/authenticated, like RLSB-05 derives its set). Bigger, schema-wide claim (affects every table, needs confirming none legitimately needs authenticated DML — true given the owner-connection architecture, ADR-0013) → **its own WP + owner greenlight**, not folded into WP-SEC-3. |
| C-30 | ✅ **DONE** (`claude/candidates-batch-1`). CI's integration job now RUNS the enforcement oracle instead of self-skipping: migration 0044 creates the anon/authenticated roles; a new `tests/helpers/ci-rls-grants.sql` (run via psql after `db:migrate`) provisions the missing SELECT grants Supabase provides (DML stays revoked per 0045/0046 → the "DELETE blocked" tests still hold); the job sets `RLS_ORACLE=1`, and the three suites now gate on `RLS_ORACLE_ENABLED = IS_SUPABASE_DB || RLS_ORACLE===1` (helper renamed from the raw `IS_SUPABASE_DB` check). A real-enforcement regression now fails CI, not just the local loop. Verified: 32/32 oracle tests green locally against the Supabase test DB post-rename (vanilla-PG CI path validated by CI itself — no local Docker). Make CI run the RLS enforcement oracle (not just skip it) | WP-SEC-2 (CI env gap) | The oracle suites (rls-behaviour/parity/grants) drive the Supabase `authenticated`/PostgREST role+grant surface and self-skip on non-Supabase DBs (`IS_SUPABASE_DB`), because CI's integration job runs a vanilla `postgres:16` service container that lacks the `authenticated`/`anon` roles (`role "authenticated" does not exist`). Migration 0044 now creates the roles idempotently (so `db:migrate` + the 0045/0046 revokes pass in CI), and rls-default-privileges (catalog-only) DOES run in CI — but the behavioural oracle only runs in the local/dev loop. To close it: either provision a Supabase-like grant baseline in the CI job (create roles + `alter default privileges … grant select`, so probeWrite's in-txn grant exercises WITH CHECK and asRole reads work), or point CI's integration job at a Supabase branch DB. Until then, a regression in the enforcement oracle is caught locally, not by CI (the pg_policies TEXT assertions still run in CI). |
| C-29 | ✅ **DONE** (`claude/candidates-batch-1`). New `tests/unit/no-client-postgrest.test.ts` (mirrors the `no-client-sentry` policy-guard shape) pins ADR-0046 Decision-6's SELECT-retained precondition: (a) no source file reads a table via `supabase.from(`, (b) no `createBrowserClient` is ever constructed, (c) no `'use client'` module imports `@supabase/{supabase-js,ssr}`. Corroborated 0 hits; the two legit server Supabase clients are auth/service-role, not `.from()` reads. Pin the "no client-side PostgREST reads" precondition with a static check | WP-SEC-3 (pr-reviewer F-2) | 0045 retains SELECT for `anon`/`authenticated` on the justification that the app never reads these tables via PostgREST (no client-side `supabase.from()`, no anon key in client bundles). That precondition is currently prose-only (ADR-0046 Decision-6, migration comment). A lightweight CI/lint check — grep for `supabase.from(` under `src/` client code, or assert `NEXT_PUBLIC_SUPABASE_ANON_KEY` never reaches a client bundle that queries these tables — would catch drift before someone adds portal-side PostgREST reads without revisiting the SELECT-retained decision. Low priority; corroborated today (grep = 0 hits). |
| C-31 | **WP-UX-SERIES** — UX/visual audit remediation, 8 slices (WP-UX-1..8) | UX audit 2026-08-16 | Screenshot-driven Fable-5 audit of the running app (report `_marketing/ux-audit-report.html` + artifact; harness `_marketing/audit-capture.mjs`). 46 findings → 5 root themes (rigid tables · no PageContainer · mobile adaptivity · color-only viz · repeated chrome). Slices + acceptance criteria in `docs/backlog/WP-UX-SERIES.md`; WP-UX-1/3/5 are MOCKUP-FIRST; WP-UX-5 carries the one Critical (mobile settings nav). Three findings refined after code verification (status-chip default, coverage panel width, tasks pager) — the report text is the corrected record. |
| C-27 | RLS/app author asymmetry: a partner cannot read admin-authored status entries at the RLS layer | WP-SEC-2 (discovered mid-build) | The `lead_status_history_scope` (and `lead_notes`/`lead_tasks`) author subquery `changed_by_user_id in (select id from users where … (role='admin' or partner_id=me))` is ITSELF filtered by `users`-RLS when run as an authenticated partner — and a partner cannot see admin USERS (users_scope: partner sees only own-org users). So the `role='admin'` arm never fires on the authenticated surface: admin-authored status entries are hidden from partners via RLS, even though the app layer (`ownStatusAuthorScope`, owner connection) correctly shows them. **Harmless today** (deny-more, no leak; the app is the source of truth and runs as owner), and pre-existing from migration 0037 — NOT introduced by 0044. Worth noting because it means the RLS backstop is intentionally more restrictive than the app for admin/system-authored child rows; if the authenticated surface is ever made load-bearing (e.g. after WP-SEC-3 keeps SELECT), revisit whether the author subqueries should reference a role-visible view instead of the RLS-filtered `users`. Covered by a test that asserts the behaviour lives at the app layer (`rls-parity.test.ts` RLP-04 admin-asymmetry). |

| C-32 | ✅ **DONE** (`claude/small-candidates`, #91). `setDone` now writes `doneAt: done ? sql\`now()\` : null` + `updatedAt: sql\`now()\`` (DB clock, one clock per column) — fixes the lead-timeline TSK-06 two-clock order flake. `setDone` stamps `doneAt`/`updatedAt` from the APP clock (`new Date()`), not the DB clock | WP-TSK-2a (surfaced) | `src/modules/tasks/tasks.ts` `setDone` writes `.set({ doneAt: done ? now : null, updatedAt: now })` with `now = new Date()`, while `created_at` defaults to Postgres `now()`. Same two-clock class C-25 swept for `updatedAt: new Date()` — this site (a `doneAt` write) was missed. Invisible until something ORDERS by the column: the lead timeline sorts `task_completed` vs `task_created` by timestamp, so when the app host's clock lags the DB (e.g. a dev machine vs the remote Supabase test DB) a just-completed task sorts BEFORE its own creation and `lead-timeline.test.ts` TSK-06 fails deterministically (confirmed on `main`, pre-existing — NOT a WP-TSK-2a regression). Fix: `doneAt: done ? sql\`now()\` : null` + `updatedAt: sql\`now()\``; the audit payload that needs the value can read it back from the RETURNING row or a `now()` select. Low-risk, mechanical; CI's local-postgres run shares one clock so it never trips there. |

| C-33 | ✅ **DONE** (`claude/small-candidates`, #91). Hoisted the hold/void time logic to `src/lib/hold.ts` (pure, client-safe) + `src/lib/hold-filter.ts` (drizzle predicate); `modules/run/{void-window,hold-window,hold-filter}.ts` are re-export shims; `lib/scope.ts` imports `releasedLeads` from `@/lib/hold-filter` — no `@/modules` dependency. `lib/scope.ts` imports `releasedLeads` from `modules/run/hold-filter` — a layering inversion | WP-TSK-2a (pr-reviewer F-4) | The foundational scope guard (imported by every API route — the highest-blast-radius file) now depends on a business module. No cycle today (`hold-filter → hold-window → void-window → drizzle/schema` only, verified), and it mirrors an existing `notes.ts`/`tasks.ts` pattern, so it was accepted for WP-TSK-2a. But `releasedLeads`/`HOLD_WINDOW_MS`/`releaseCutoff` are pure, tiny primitives with no `run`-specific behaviour. Cheap hardening: hoist `hold-filter.ts`/`hold-window.ts`/`void-window.ts` into `src/lib/hold.ts`, re-export from `modules/run` for existing call sites, so `lib/scope.ts` depends only on other `lib/` primitives. Not urgent; no behaviour change. |

| C-34 | **WP-AUTH-OUTAGE-2** — extend SEC-09 (503+Retry-After) to `otp/verify` + `trust/refresh` `session_failed` | WP-AUTH-OUTAGE (audit-security F-1) | Both routes return `jsonError("session_failed", …, 500)` when `establishSessionForEmail` returns false — which `otp-session.ts` documents as an INFRA problem (bad service-role key / project URL / Supabase outage), i.e. the same account-independent, retryable class login/change-password now signal as **503** (SEC-09). Not silent (it logs `otp_session_establish_failed`), and it runs AFTER the code/token is validated so there's no enumeration/lockout angle — hence a consistency gap, not a bug. Fix: route these through `jsonServiceUnavailable`, which needs `establishSessionForEmail` to return a tri-state (or throw a typed InfraError) so the route can tell a transient session-mint failure from a real one. Small; its own WP. |
| C-35 | Backfill the missing drizzle snapshot files for migrations 0036, 0037, 0044–0047 | WP-AUTH-OUTAGE (audit-data F-5) | Those five applied, prod-verified migrations are RLS-policy / GRANT-REVOKE-only SQL (hand-authored, no `drizzle-kit generate`), so their `meta/NNNN_snapshot.json` files were never committed — the chain jumps 0043→0048. Harmless TODAY (none change table/column/index structure, and `schema.ts` declares no `pgPolicy` objects for drizzle to track), but it's real ledger drift: the next migration that touches a table whose structure changed in one of those commits (none do currently) would diff `drizzle-kit generate` against a stale baseline and could emit a spurious drop+create. Regenerate + backfill the 5 snapshots, OR document that these are intentionally SQL-only migrations that bypass generate. Not a blocker; PROTOCOL §11 ledger-drift watch item. |

| C-36 | Add the `notifications (tenant_id, lead_ref) WHERE lead_ref is not null` partial index via CONCURRENTLY | WP-RET-3a (pr-reviewer F-1) | WP-RET-3a's redaction looks up notifications by (tenant, lead_ref); the index was DROPPED from migration 0049 because a plain `CREATE INDEX` on the now-live table takes a write-blocking ShareLock (forbidden post-launch, migrations 0027 / WP-J2 spec) and drizzle's transactional migrate can't run `CONCURRENTLY`. notifications is small today so the redaction seq-scans fine (same cost model as the other operational sweeps). When volume warrants, add the partial index via `CREATE INDEX CONCURRENTLY … ` applied OUT-OF-TRANSACTION (manually to prod, the way 0036/0037 were reconciled, or once a non-transactional migration path exists). **Promote the repo's twice-written rule** ("index/column adds on a populated prod table use CONCURRENTLY out-of-transaction; drizzle single-file-txn means split into two files") into a real ADR (a fresh DM-xx id — DM-12 is taken). |
| C-37 | Fold notifications/outbox redaction counts into the per-lead `lead.pii_purged` audit row | WP-RET-3a (pr-reviewer F-4) | `sweepTenantPii`'s per-lead audit row records `notesRedacted`/`tasksRedacted` but not the new `notificationsRedacted`/`outboxRedacted` (those are only in the aggregate cron response). Minor LGL-02 evidentiary gap — an auditor reconstructing "what was redacted for lead X" from `audit_log` alone won't see comms counts. Needs `redactLeadCommunications` to return a per-lead breakdown (or a second per-lead pass). Low priority. |

| C-38 | Add a void-path cross-tenant collision test for `redactLeadCommunications` | WP-RET-3a (audit-tenancy F-1) | `retention.test.ts` proves the SWEEP path can't cross the tenant wall on a colliding refId (tenant B's notification/outbox with the same LD-26-00001 survive an A sweep). The VOID path uses the same shared helper with its own `scope.tenantId` (verified by reading), but `void.test.ts` has only one tenant, so a second-tenant collision isn't directly exercised there. Low — the shared helper's isolation is proven once; this is coverage symmetry. Add a 2nd tenant + same-refId notification/outbox to void.test and assert they survive `voidUpload`. |

| C-39 | Audit the erasure runbook for completeness against ALL server-side PII sinks (esp. AI surfaces) | WP-RET-3b (pr-reviewer compliance hint) | `docs/ops/erasure-runbook.md` enumerates the 7 lead-PII locations C-13 touched, but a full compliance sweep should confirm no OTHER server-side sink holds seller PII — notably the AI assistant (`ai_memory`, and whether assistant answers/transcripts persist lead content server-side; ADR-0041 says transcripts are CLIENT-persisted, and `ai_feedback` is already 90d-swept, but confirm `ai_memory` doesn't retain lead free-text). If any exist, add a runbook row + a purge/sweep path. An `audit-compliance` pass over the runbook is the right vehicle. |

| C-40 | **WP-RET-4** — erase the three server-side seller-PII sinks the purge paths miss (found by the C-39 audit) | WP-RET-3b / C-39 (audit-compliance) | Three sinks survive a void/purge today, all verified against code 2026-08-17: **(a) HIGH — Supabase Storage `run-exports/{tenantId}/{uploadRef}.xlsx`:** `storeExport` (`src/modules/export/storage.ts`) writes the full rendered deliverable (every lead's seller PII) to a private bucket; the download route blocks a *voided* run (`app/api/runs/[ref]/export/route.ts` — its own comment admits the blob "would still carry recalled leads' PII"), but nothing DELETES the object — `voidUpload`/`sweepTenantPii` are DB-only. This directly breaks the runbook's "void = PII gone at once" promise (LGL-02/DM-09). Fix: best-effort `admin.storage.from("run-exports").remove([path])` inside/after `voidUpload` (same post-tx pattern as the digest send) + a backstop sweep that removes exports for uploads whose leads are all `pii_purged_at IS NOT NULL`. **(b) MEDIUM — `listing_checks.result` (jsonb `{link}`):** `LinkOnlyProvider` (`src/modules/listing/link-only.ts`) stores a Google-search URL embedding the full street address, one row per kept lead by `leadId`; neither purge path references `listing_checks`. Fix: `set result = null` for the purged lead ids in both `voidUpload` and `sweepTenantPii` (direct `leadId` FK — no ref-string correlation). **(c) LOW — `leads.mlsMatchSpan.text`:** the MLS matcher captures a verbatim fragment of `leads.notes` (`src/modules/pipeline/mls.ts`) into the jsonb column (`schema.ts:294`); `redactionPatch()` nulls `notes` but not `mlsMatchSpan`. Bound low by PRN-04 (patterns anchor to short listing-status tokens), but it's an unredacted copy of a column the runbook already classifies as PII. Fix: add `mlsMatchSpan: null` to `LeadRedactionPatch`/`redactionPatch()` (`src/modules/retention/purge.ts`). All three documented as known gaps in `docs/ops/erasure-runbook.md` (rows 8/9 + row-1 caveat). (b)+(c) are small DB-only redaction adds; (a) is the WP's substance (a new Storage-deletion path + backstop) and is **Tier A-adjacent** (touches prod Storage) → owner greenlight. |
| C-41 | ✅ **(a)(b)(d) DONE — PR #128 (2026-08-18); (c) still deferred.** (a) one canonical portal-leads key/URL (`portalLeadsKey`/`portalLeadsUrl`), mobile gated on the resolved viewport (`useDesktopState` 3-state), dashboard preview desktop-only — one fetch per view, cache-share real; (b) both lead dialogs seed `placeholderData` from the list row via reshape builders (never `initialData`; phone/email never fabricated; Edit disabled while partial); (d) `/api/leads/counts` ({total,unmatched}, one `count FILTER` round trip composing the scoped predicates) replaced both nav-count routes — incidentally fixed the unmatched-badge invalidation miss (old `["unmatched","count"]` key was outside the `["leads"]` invalidation family). **(c) admin loading boundary remains deferred:** the layout awaits `getServerScope` above any possible boundary; moving the auth gate is its own decision. **Frontend perf follow-ups** (from the Slice-2 frontend audit; the quick wins — keepPreviousData ×4, /coverage code-split — shipped in `claude/perf-waterfalls-slice2`) | Slice-2 perf audit 2026-08-18 | Four remaining, each own-diff: **(a)** portal leads fires DUPLICATE fetches — `LeadsMobile` mounts+fetches under `["portal-leads", filterKey, page]` on desktop first paint (SSR `useIsDesktop()`=false) before `LeadsDesktop` fetches under `["portal-leads-desktop", …]`; and the dashboard preview's `["portal-leads", 1]` (2 elems) never matches either 3-elem key, so its claimed cache-share never happens (`portal-leads-view.tsx`, `leads-desktop.tsx:61`, `leads-mobile.tsx:58`, `portal-dashboard.tsx:76`). Align keys / collapse onto one shared hook; portal-leads tests pin these keys → update together. **(b)** lead detail dialogs (`lead-dialog.tsx:151`, `portal-lead-dialog.tsx:59`) always full-round-trip + 6-bar skeleton though the clicked row already has seller/address/status/partner/tags/score in the list cache → seed `initialData` from `queryClient.getQueryData(["leads", …])`, background-refetch the rest. **(c)** no `(admin)/loading.tsx` (portal has one) — but the admin layout awaits `getServerScope` ABOVE the loading boundary and pages render their own `AppShell`, so a bare skeleton floats without nav; wants a shell-aware skeleton or moving the scope gate below a boundary. **(d)** merge the two nav-badge count endpoints (`/api/leads/count` + `/api/leads/unmatched/count`, fired every admin page by `AppShell.tsx:99`) into one so page-load resolves scope once not twice (compounds with C-42). All Tier B. |
| C-42 | **WP-PERF-AUTH** — every `/api/*` verifies the JWT TWICE over the network | Slice-2 backend audit 2026-08-18 | `proxy.ts:92` (middleware, runs on every non-static request incl. `/api/*`) calls `supabase.auth.getUser()`, then the route's `getServerScope()` (`scope-context.ts:85`) calls it AGAIN. `getUser()` is a GoTrue HTTP round trip (verifies server-side), NOT a local JWT decode — so every API call pays ~2 auth RTTs (against Frankfurt, ~150ms each) before touching Postgres. This is a constant per-request tax on top of every screen. **NOTE: the `fra1` region pin (Slice 2, shipped) drops each getUser from ~150ms to ~local, so this is deprioritized while co-located — revisit if the DB moves to US.** Fix options: (1) **local verification** — enable Supabase asymmetric JWT signing keys + switch to `getClaims()` (verifies the signature locally against a cached JWKS, no network); (2) have the middleware pass its already-verified identity to the route so `getServerScope` doesn't re-verify. Both touch auth → **Tier A, owner greenlight + audit-security pass**. |

| C-43 | **WP-KAN-1b** — denormalize `current_status` onto `leads` for the status board/list at 100k+ (the durable fix; WP-KAN-1a step-1 LATERAL is the stopgap) | Slice-2 perf re-measure 2026-08-18 (owner flagged 10k–200k+) | **Measured board query (test DB, EXPLAIN ANALYZE):** today 1,060 ms @ 50k / ~6.7 s @ 200k; LATERAL+covering-index (shipped, WP-KAN-1a) 508 ms @ 50k but **6.7 s @ 200k** (200k random index probes exceed cache → disk); DISTINCT-ON set-based 5.4 s @ 200k (still materializes+sorts all N); **denormalized `current_status` column + index = ~1.5 s @ 200k unoptimized, → sub-second with a `(tenant_id, current_status, status_since desc) where kept` index + VACUUM (index-only totals + per-column top-25 via the index).** So for 100k+ the board is O(n) on history-derived status and ONLY denormalization breaks it. **Conflicts with PRN-15** ("derived values… never duplicated or dual-written") → needs an **ADR** framing `current_status` as a synchronized projection updated in the SAME transaction as every status write (single source of truth = history; column = maintained cache). Scope: (a) add `current_status` (+ `current_status_at`) NOT NULL default 'New' to `leads` + backfill migration; (b) maintain it on every status change — `portal/status-update.ts` + the admin status write — in-tx; (c) redesign the board (per-column indexed totals + top-25) and the list status filter/sort to read the column; portal list + global search likewise; (d) analytics "currently untouched/contacted" KPIs can read it too. Tier A (schema + write path + prod migration). Trigger: a tenant approaching ~80k, or build now if 100k+ is imminent (owner: scale is unknown, 20k–200k+). |

| C-44 | ✅ **DONE (PR #134, WP-N1)** — `canDo` optional-chains + fails closed; unit legs added. Harden `useCurrentUser` against a capabilities-less `/api/me` payload | Slice-3 C-11 build 2026-08-18 | `src/lib/use-current-user.ts` `canDo` reads `query.data?.capabilities.includes(cap)` — if the payload ever arrives without `capabilities` (old cached response, contract drift) the render CRASHES rather than failing closed. One optional-chain (`capabilities?.includes(cap) ?? false`) fixes it; add a unit leg. Found while wiring C-11's "You" rule; out of that WP's scope so only the test mocks were corrected. |
| C-45 | ✅ **DONE (PR #133, WP-N2)** — (a) AIS-10 post-answer follow-up chips; (b) AIS-11 `get_recent_activity` masked projection, gated `ops.admin` (audit-tenancy: `ai.use` alone would have exposed ADMIN_LOCKED data to member seats); (c) AIS-12 rate-gate 60s auto-clear. AI assistant follow-ups (deferred from WP-AI-STYLE, PR #125) | WP-AI-STYLE 2026-08-18 | Three small, independent: (a) **WP-AI-CHIPS-2** — post-answer follow-up chips (the empty-state chips shipped; a bounded post-answer row was specced and deliberately deferred); (b) **WP-AI-TOOL-ACTIVITY** — a masked audit-trail tool so the Activity screen can earn native chips (blocked on a mask projection for actor emails, SEC-05); (c) rate-gate auto-clear ~60s to match the 429 copy's "give it a minute" (cosmetic). |
| C-46 | ✅ **DONE (PR #134, WP-N1)** — same-stream active roster (`GET /api/tasks/assignees`, work.write + ToS), Select in add+edit forms defaulting “Me”, `resolveAssignee` now refuses deactivated seats, off-roster assignee renders truthfully from the row's own C-11 identity. Task assignee PICKER (TSK-03 v2) | C-11 2026-08-18 | v1 is self-assign only. C-11's payload now carries both identities and `resolveAssignee` already validates any same-stream target — a picker slots in without another payload change. Needs: a same-stream user-roster read for the dropdown (member/viewer visibility question), the TasksPanel row affordance, and the TSK-03 default-to-creator rule kept. Owner call on whether v2 is worth it before real multi-seat usage. |
| C-47 | ✅ **DONE (PR #134, WP-N1)** — `streamUsersWhere`/`sameStreamUsersWhere` in scope.ts consumed by all four sites (tasks module, noteWhere/taskWhere ownAuthors, statusAuthorOrg as OR-of-arms, still raw-SQL-composable); SQL-equivalent, scope suites green unmodified; house rule landed in ENGINEERING_STANDARDS §2. Promote `sameStreamUsers` into `lib/scope.ts` | audit-tenancy on C-11 (F-1) | The own-stream user-set predicate now lives in `src/modules/tasks/tasks.ts` (marked ⚠️ SCOPE-GUARD-ADJACENT) while three sibling definitions live in the guard (`noteWhere`/`taskWhere` `ownAuthors`, `statusAuthorOrg`). Promote one `sameStreamUsersWhere` builder into scope.ts and have all four compose it, so a stream-tier change lands once. Pairs with the audit's proposed ENGINEERING_STANDARDS §2 rule ("a predicate deciding which users belong to the caller's stream is a scope builder"). |
| C-48 | ◐ **N3 progress (PRs #140/#142):** §1.2 mobile phantom tile ✅ (last KPI tile `max-sm:col-span-2`, both dashboard grids) · §12.1 coverage floating card ✅ (MapCaption `max-sm:hidden` everywhere, =Q10) · §12.2 pointer-aware map copy ✅ (gated with C-57). Remaining: §2.1 Clear-all placement · §6.1 DueChip dedup · §7.1 activity filter grammar · §8.1/§8.2 rules polish · §11.3 settings pill-rail fade (ScrollHintFade now exists — cheap) · §1.3 donut rounding · §1.4/§5.1 duration precision. Fresh-audit small polish batch (the un-shipped remainder of `ux-audit-fresh.md`) | Scope-E closeout 2026-08-18 | Shipped: §3.1/§3.2/§5.1 (PR #108), §4.2/§1.1/§10.1/§10.2/§2.2/§11.1 (PRs #124/#126). Remaining smalls, one batch-PR: §2.1 leads "Clear all" placement (chip row, not its own wrap line) · §6.1 DueChip group-header dedup · §7.1 Activity filter-bar grammar (inline placeholders like every sibling) · §8.1/§8.2 Rules lock-note placement + compact empty state · §11.3 settings pill-rail edge-fade scroll hint · §12.1/§12.2 coverage floating-card hide on /coverage + pointer-aware map copy · §1.3 donut largest-remainder rounding (in `src/modules/analytics`, PRN-15) · §1.2 mobile stat-grid phantom tiles (odd tile spans the row) · §1.4/§5.1 shared duration-precision rule. DEFER queue (owner) unchanged: §5.2 urgency tiers, §6.2 tasks partner context, §9.1 import outcome columns, §10.3 territory banner retirement, §11.2 tag rename, §4.3 unscored-card collapse. |
| C-49 | ✅ **DONE (PR #140).** partners/[id] adopted `coverageSummary()` (+ render test). The coverage/page.tsx half was VERIFIED STALE — its legend filters `stateCount > 0` (map.ts:121) and prints states only, so no zero ever renders there; legend untouched. `coverageSummary` sweep at the two remaining unconditional sites | Scope-E review (F-1 note) | PR #124 fixed the partners roster + House tile; `partners/[id]/page.tsx:~178` and `coverage/page.tsx:~144` still hard-print the "0 ZIPs · " pattern. Mechanical adoption of `lib/coverage-summary.ts`. |
| C-50 | Advisory-lock namespace ADR (two-arg form) — only if the lock roster grows | C-24 reviews | Six lock families now share one `hashtext(text)::bigint` space (bare tenant, `:partner`, `:retention`, `:tags`…). Collision odds are ~2^-32 and consequences are serialization-not-corruption, but if more locks land, an ADR moving to `pg_advisory_xact_lock(classid, objid)` gives true namespaces. Low. |

<!-- ── Deep UI/UX audit 2026-08-19 (report: _marketing/audit/deep-ux-audit-2026-08-19.md; rubric+captures: _marketing/audit-2/). 0 Critical, 3 High, 17 Med, 19 Low. ── -->
| C-51 | ✅ **DONE (PR #140).** `Skeleton as="span"` + both hero sites fixed + app-wide sweep (99 elements/40 files; 3 more violations found+fixed in portal-lead-dialog). **HIGH — Portal dashboard hydration error: `<Skeleton>` (a `<div>`) rendered inside `<p>` AND `<h2>`** | UX audit 2026-08-19 (OF-1 / F-PORTAL-hydration) | `src/app/portal/dashboard/portal-dashboard.tsx:139-141` (mobile hero, in `<p>`) and `:197-199` (desktop hero, in `<h2>`) render `Skeleton` (renders a `<div>`, `Skeleton.tsx:6-13`) during load. `<div>`-in-phrasing is invalid → SSR/client tree diverge → React throws a hydration error + re-renders the hero on **every cold load** of the partner landing page. Confirmed by runtime console `pageerror` in a route sweep (admin routes are all clean; only this one throws). Fix: `Skeleton as="span"`/`inline-block`, or make the skeleton a sibling replacing the `<p>`/`<h2>` rather than a child. Apply to both sites. |
| C-52 | ✅ **DONE (PR #141).** Layout-neutral `before:` pseudo-element hit areas: Checkbox 26px, Dialog ✕ 30px (46px coarse), FilterPill ~28px, swatches 24px (row gap 6→8px), bell link 28px; 44px coarse-pointer applied only where dense rows can't steal taps (→ C-85). **Sub-24px pointer targets (WCAG 2.5.8 AA) — batch, mostly shared primitives** | UX audit 2026-08-19 (T-TARGET: F-GL-1/F-LD-2/F-settings-4/F-OV-2/F-PORTAL-2) | Recurs across five controls: shared `Checkbox` 16px box no padded hit area (`Checkbox.tsx:64`); shared `Dialog` close ✕ ~18px no padding (`Dialog.tsx:86-93`); tag color swatches 16–20px/~22px spacing, sole recolor ×53 rows (`tags/page.tsx:262-269`); bell "Mark all read" ~16px text link (`NotificationBell.tsx:117-121`); portal mobile status chips ~22px on the *contractual* 375 surface (`FilterPill.tsx:28-34`). Fix: add invisible ≥24px (≥44px coarse-pointer) hit padding to the shared `Checkbox`/`Dialog`-close/`FilterPill` primitives + the two bespoke controls — one primitive pass clears most. |
| C-53 | ✅ **DONE (PR #141).** Fade recipe lifted into shared `ScrollHint` (`useScrollHint`+`ScrollHintFade`, later grew a y-axis in #142); adopted with min-w 760/640/560 + portal chip strip. Adopt `Table` `scrollHint` + `min-w` on leads / unmatched / imports (+ portal chip row) | UX audit 2026-08-19 (T-SCROLLHINT: F-LB-2/F-UNM-1/F-IMP-1/F-PORTAL-3) | The `Table` primitive already ships an opt-in edge-fade `scrollHint` (dashboard perf tables use it), but three tables never opted in, so columns clip silently with no cue on narrow viewports: leads (`leads-view.tsx:492`), unmatched — clips Waiting+Assign, the whole decision (`unmatched/page.tsx:427`), imports — FILE starves to "w…" with a hover-only `title` (`imports/page.tsx:113`). Portal mobile status-chip row also lacks the fade (`leads-mobile.tsx:79`). Mechanical adoption of the dashboard precedent + a sensible `min-w`. |
| C-54 | ✅ **DONE (PR #141).** Promoted `ClearFiltersButton` (4th duplication rule); leads reuses the SV-04 apply channel via `seedFrom(f)`; site-level wiring test added in review round. Filtered-to-zero empty states need a Clear-filters action (leads / unmatched / imports) | UX audit 2026-08-19 (T-CLEAR: F-LB-1/F-UNM-2) | Activity's filtered-empty ships a "Clear filters" button; the identical pattern is missing on leads (`leads-view.tsx:490`), unmatched (`unmatched/page.tsx:424`), imports (`imports/page.tsx:86`) — each tells the user to "widen the filters" with no control. `EmptyState` already supports an `action` slot. Pass Activity's button when `hasFilters`. |
| C-55 | ✅ **DONE (PR #140, owner-greenlit).** Public `/terms` (renders the SAME `lib/legal/tos.ts` text the gates use — attorney text swaps in one place; APP_NAME identity block added in review round); signup link `target=_blank rel=noopener`; "Read the full terms" on both gates; ⚠️ placeholder text is now publicly visible + no footer link yet (→ Deferred/C-88). **HIGH — Signup Terms link dead-ends signed-out users + loses their form** | UX audit 2026-08-19 (F-AUTH-1) | `signup/page.tsx:261` links the *required* consent text to `/tos`, a protected page (`proxy.ts:26`); a signed-out prospect who clicks to read the terms is redirected to `/login` (`proxy.ts:114-117`), never sees them, and loses the typed email/password/workspace/invite. The one mandatory legal affordance is broken for its entire audience. Fix: a genuinely public terms surface opened in a new tab (`target="_blank" rel="noopener"`) so the form survives. |
| C-56 | ✅ **DONE (PR #143).** Partners page split into server shell + `partners-view.tsx`; `?edit=<id>` (UUID-parsed, one-shot seed, roster-scoped — foreign id inert); "Edit partner" relabel + Admin-notes CTA. Partner detail is a read-only dead end — "Edit on Partners →" bounces to the unfiltered roster | UX audit 2026-08-19 (F-partners-1) | `partners/[id]/page.tsx:183-188` — the detail page's only action is a `<Link href="/partners">` to the roster (no partner id / deep-link; the list's edit is local `useState`). Every mutation on the partner you're viewing (edit contact, add the notes the empty-state names, invite, reassign, deactivate) means back → re-scan 14 rows → ⋯ → Edit. Fix: lift the roster's edit state into a URL param (`/partners?edit=<id>`), link the button there, relabel "Edit partner", and reuse it for the Admin-notes empty-state CTA. |
| C-57 | ✅ **DONE (PR #140).** `interactive={isDesktop}` mirrored from the dashboard + pointer-copy sentence gated too. Note: static mode also disables tap-to-highlight-a-partner on phones (Partners list stays the touch path — same trade the dashboard made). Coverage county map traps touch-scroll on phones (gate `interactive={isDesktop}`) | UX audit 2026-08-19 (F-COV-1) | `coverage/page.tsx:91-97` mounts `CountyCoverageMap` with no `interactive` prop → defaults true → `touch-action:none` (`CountyCoverageMap.tsx:262`) swallows page-scroll that starts over the tall map. The dashboard already mitigates the identical hazard with `interactive={isDesktop}` (`dashboard/page.tsx:254`); coverage never adopted it. Fix: mirror the dashboard's `useIsDesktop()` gate. |
| C-58 | ✅ **DONE (PR #142).** `NotesPanel variant="section"` byte-matches the ScorePanel shell; default card look preserved elsewhere. Lead-dialog "Admin notes" breaks the dialog's unified section chrome | UX audit 2026-08-19 (F-LD-1) | Score/Tasks/Timeline render as uppercase headers on muted `surface-2` panels; "Admin notes" alone uses a white `Card`/`CardTitle` (title-case) (`lead-dialog.tsx:360-362`, `NotesPanel.tsx:103-105`). WP-UX-7's header unification didn't reach the notes block. Fix: give NotesPanel a `variant="section"` (or lift the header/wrapper into lead-dialog) so all four sections share the panel treatment. |
| C-59 | Lead dialog prev/next "N of M" record navigation (enhancement) | UX audit 2026-08-19 (F-LD-3) | Triage is open-read-close-reopen with no orientation ("3 of 686"); `leads-view.tsx` already owns the ordered/filtered/paged row list and drives `openRef`, so a pager is cheap here. Twenty does keyset N-of-M cached during refetch (`useRecordShowPagePagination.ts`). Owner product call — recommend, don't auto-build. |
| C-60 | ✅ **DONE (PR #143).** `listMyTasks` returns `groupTotals` + `today` over the SAME `where` in the same round trip (drift-guard test renders the real drizzle SQL vs `groupByDue`); badge/headers read server totals; "N of the overdue tasks are on another page" disclosure when the badge outruns the page (jump-to-page → C-89). My Tasks grouped counts + "N overdue" badge are page-scoped → mislead at scale | UX audit 2026-08-19 (F-tasks-1) | `MyTasksList.tsx:145-149` computes group counts + the overdue badge from the current page only (page size 20, `schema.ts:16`); ordered `dueOn asc nulls last`, so the "No due date" bucket surfaces only on the last page and the badge undercounts + re-computes as a >20-task user pages. In-code comment already defers a cross-page rollup as a future WP. Fix: return per-group + true overdue totals from `/api/tasks`; render badge/headers from server totals while rows stay paginated. |
| C-61 | ✅ **(a)–(d) DONE (PR #142); (e) deferred (owner eyeball).** Page section retitled "Workspace" (nav label wins — sole outlier); AI card title deduped; AI toggle gained the Data toggle's "Saved." toast (+ negative-leg test); "No email" casing unified. (e) coverage StatCard anatomy vs HeroKpi NOT built — design change, owner call. Settings consistency + copy batch (Workspace→General title, AI card dup, toggle save-feedback, partner "no email" case) | UX audit 2026-08-19 (F-settings-2/3/5, F-partners-2, F-COV-2) | (a) "Workspace" nav lands on a page titled "General" — lone nav↔title mismatch (`settings-nav.tsx:23` vs `workspace/page.tsx:50`). (b) AI card title duplicates its section title verbatim (`ai/page.tsx:7` + `ai-settings.tsx:125`). (c) AI enable toggle saves silently while the Data toggle shows "Saving…/Saved." (`ai-settings.tsx:63-67` vs `data/page.tsx:40-43`). (d) "no email"/"No email" list vs detail (`partners/page.tsx:771` vs `[id]/page.tsx:175`). (e) Coverage stat-tile order/casing vs dashboard HeroKpi. All small consistency reconciliations toward shared primitives / one house style. |
| C-62 | Assistant mobile bottom sheet has no scrim / focus trap (tap-through) | UX audit 2026-08-19 (F-OV-1) | `AssistantWidget.tsx:197-213` — on `max-[520px]` the panel is a bare `fixed` bottom sheet with no backdrop: undimmed live content, no tap-outside-to-close (Esc needs a keyboard), and no focus trap while open (`inert` only when closed) so focus/pointer fall through to the page behind. Global search does this correctly via `Dialog`+scrim. Fix: render a dismiss scrim + lock body scroll + focus-trap on the mobile sheet (owner-gated on the modal-vs-persistent question). |
| C-63 | ✅ **DONE (PR #142).** Shared `AuthCardHeader` (h1 = purpose, APP_NAME eyebrow) on all 9 auth-adjacent screens incl. /terms + both ToS gates (which rejoined the centered card identity). Auth-screen heading + ToS-gate identity consistency | UX audit 2026-08-19 (F-AUTH-2/F-AUTH-3) | (a) Only login has an `<h1>` (and it's the brand, not the purpose); signup/forgot/reset/verify render their titles as `<span>` with no heading landmark (`login-form.tsx:53` vs the others). (b) The ToS gate screens (`(admin)/tos/page.tsx:44-45`, `portal/tos`) drop the product brand and use a wider top-aligned layout, breaking the shared centered auth-card identity. Fix: one auth-card header primitive (`<h1>` = screen purpose + brand sibling) reused across all auth screens incl. both ToS gates. |
| C-64 | ✅ **DISSOLVED** — (a) chip target → C-52 ✅ (#141) · (b) chip-row fade → C-53 ✅ (#141) · (c) dialog bottom scroll cue → C-65 ✅ (#142). Portal mobile polish batch (chip target, chip-row fade, dialog scroll cue) | UX audit 2026-08-19 (F-PORTAL-2/3/4) | Portal is contractual at 375 (FS §9). (a) Status chips ~22px, under 44px (`FilterPill.tsx:28-34` — folds into C-52). (b) Mobile status-chip scroll row has no edge-fade cue (`leads-mobile.tsx:79` — folds into C-53). (c) Portal lead dialog (shared `Dialog`) has no bottom scroll cue on long stacked content, so a partner can miss the Notes/Timeline below the fold (`Dialog.tsx:75,82`; `portal-lead-dialog.tsx`). |
| C-65 | ✅ **DONE (PR #142).** Dialog title bar + footer pinned outside a body-only scroller; bottom fade via ScrollHint's new y-axis (x-path byte-identical, content-observer gated to y); FRM-02a overlay/bare/focus-trap preserved, all 18 call sites swept; lead-dialog partial skeletons now panel-shaped with real headers. Lead-dialog scroll-cue + partial-skeleton fidelity | UX audit 2026-08-19 (F-LD-4/F-LD-5) | (a) The shared `Dialog` scrolls its title+✕ together with the body (`Dialog.tsx:71-103`), so on a tall lead they scroll off; add a pinned title bar + bottom fade cue. (b) Score/Timeline partial skeletons are flat `h-28`/`h-24` blocks (`lead-dialog.tsx:351,358`) vs the labelled field skeletons above; give them the real panel shell + uppercase header (like TasksPanel's skeleton). |
| C-66 | ✅ **DONE (PR #142).** `title=` on the truncated task context; activity skeleton is column-structured rows. Truncation-recovery + skeleton fidelity smalls (tasks, activity) | UX audit 2026-08-19 (F-tasks-2/F-activity-1) | (a) My Tasks seller/city context truncates with no `title` (`MyTasksList.tsx:293-296`) — undoes WP-UX-7's identity enrichment on narrow widths; add `title`. (b) Activity loading skeleton is 6 full-width bars, not the 5-column grid structure (`activity/page.tsx:99-100`); render column-structured skeleton `Tr`s. |
| C-67 | ✅ **DONE (PR #142).** ".xlsx or .csv" dropzone copy; "Download template" inside the unrecognized-format card (sentence now points "below"). Upload copy + template discoverability | UX audit 2026-08-19 (F-UP-1/F-UP-2) | (a) Dropzone says "Drop a weekly .xlsx here" but `.csv` is accepted (`upload/page.tsx:201` vs `:140`, guard names both) — align to ".xlsx or .csv". (b) The template download is a lone `text-xs` header link the unrecognized-format error only *references* ("above"); also surface a "Download template" button inside that error card (near `:189`) so recovery is one click from the failure. |
| C-68 | ✅ **DONE (PR #142).** Active-sort header renders `text-text` (glyph change already accompanies color — PRN-14); aria-sort untouched. Leads table: emphasize the active sort column | UX audit 2026-08-19 (F-LB-3) | `Table.tsx:134-136` shows the sort direction as a `↕`/`↓` glyph in the same muted `text-text-3` as inactive headers; `aria-sort` is set (SR ok) but at a glance it's hard to tell which column orders the table. Fix: render the active column's arrow (and/or label) in `text-text`/`text-brand-ink` and/or semibold when `sortDir !== null`. |
| C-69 | ✅ **DONE (PR #143).** "View all in Leads →" → `/leads?partnerId=<id>` via shared `partnerIdParam()` (also deduped into LeadsQuerySchema/BoardQuerySchema); foreign id degrades to an empty 200 (no existence oracle). Partner detail "Recent leads · last 25" caps with no path to full history | UX audit 2026-08-19 (F-partners-3) | `partners/[id]/page.tsx:271-302` renders a fixed 25-row window with no pager/link; a high-volume partner's older leads are unreachable from their profile (individual rows do drill into the shared dialog). Fix: a "View all in Leads →" affordance pre-filtered to this partner, or a load-more. |
| C-70 | ✅ **DONE (PR #142).** Confirm-password validates on blur+submit (submit reachable, request hard-blocked on mismatch — GOV.UK; owner note: button no longer disabled by an unrevealed error); `!siteKey` copy → "Signups are temporarily unavailable." Auth low-severity smalls (per-keystroke validation, Turnstile dead end) | UX audit 2026-08-19 (F-AUTH-4/F-AUTH-5) | (a) Reset confirm-password validates on every keystroke — "Passwords do not match." flashes char-by-char (`reset/page.tsx:29`); defer to blur+submit (GOV.UK). (b) The Turnstile `!siteKey` state disables signup with "try again later" for a *persistent* config gap (`signup/page.tsx:238-240,272`); make it an explicit "Signups temporarily unavailable" tied to the disabled button (mainly non-prod). |

<!-- ── N-slices closeout 2026-08-19 (WP-N1 #134 · WP-NF1 #131/#132 · WP-N2 #133) — candidates minted from implementation + review rounds. C-51–C-70 above were minted concurrently by the deep-UX-audit session. ── -->
| C-71 | `resolvePref` returns `undefined` for a valid-typed role×event miss | WP-NF1 D4 build | `NotifEvent` is one union across BOTH roles, so `resolvePref(prefs, "admin", "assigned_lead")` typechecks and crashes on `.inApp` at runtime (same for `admin`+`new_leads`, pre-existing). Fix: per-role event unions, or a total fallback in `resolvePref`. |
| C-72 | Outbox drain wall-clock budget | pr-reviewer on #131 (F-1) | The 10s Resend `AbortSignal.timeout` stops a HUNG send; a uniformly SLOW provider (e.g. 8s/send × 100-row limit, sequential) can still outlive the 60s cron budget. Mirror `remindDueTasks`' `deadlineMs`/`clockMs` budget in `drainOutbox`. |
| C-73 | `lead_tasks_scope` WITH CHECK assignee parity | audit-tenancy on #134 (F-2) | The RLS twin's `assigned_to_user_id` subquery is tenant-only (0047/0054) while app-side `resolveAssignee` now also pins stream + active seat. Safe direction (app stricter), but bring the WITH CHECK up to tenant + stream + `deactivated_at is null` in the next RLS migration + an rls-parity leg. scope.ts comment already states the delta. |
| C-74 | AI tool-level capability map (draft AIA-06) | audit-tenancy on #133 (F-1 generalization) | The `ops.admin` gate fixed the audit-trail tool, but every OTHER tool still rides `ai.use` alone — a member reaches `get_partner_performance`/`get_partner_territory`/`list_partners`, whose screens gate on `partners.manage`. Wanted: one `Record<toolName, Capability>` filtered at build time + an AUTHZ-style partition test that fails when a new tool has no capability row. Proposed rule: “`ai.use` grants the assistant, never data a seat could not reach through the UI.” |
| C-75 | Partner-seat notification volume at scale | WP-NF1 D3 | Every active seat now gets an in-app row per digest/hot/assign event (correct at today's 1–2 seats). A 20-seat org needs per-seat mute or a role-scoped default before it's spam. Related owner flip: `assigned_lead` email default is OFF (one line to change — the toggle now genuinely works). |
| C-76 | Muted-stream due tasks can crowd the reminder sweep window | WP-NF1 D5 (audit-tenancy F-7 note) | Skip-not-claim means a muted stream's due tasks stay in the sweep's `limit(200)` window every tick (ordered dueOn asc). A tenant with one stream muted and >200 due tasks in it could starve the other stream's nudges. Edge-case; bound it (per-stream sub-limits or a muted-stream predicate in the due select) if real tenants mute one stream long-term. |
| C-77 | Test-infra flakes: Windows vitest worker starts + AI-suite pooler resets | WP-N1/N2/NF1 builds | (a) `pnpm test:unit` at default parallelism throws “failed to start forks worker” on this machine (files never run; green at `--maxWorkers=4`) — pin `poolOptions` in vitest.config so a local run isn't a false red. (b) The four AI integration suites run together intermittently `ECONNRESET` against the remote pooler; a per-file `max: 1` postgres client or retry wrapper. |
| C-78 | App-side ref linkification (the safe inverse of typed record references) | WP-N2 deferral analysis (PR #133) | Model-emitted link syntax (`[[lead:LD-…]]`) would move link construction from the whitelisted server-side `path` into model-authored text — the boundary PRN-10 holds. The safe version: the APP linkifies LD-/PR-/IM- refs it can resolve from that turn's own tool results. Own WP with tests. |
| C-79 | Assistant activity follow-ons: actor identity + entityType glossary | audit-tenancy/pr-reviewer on #133 | (a) The tool masks actor emails, so “who changed coverage last week” is unanswerable — unmasking staff identity to the model is a deliberate owner decision, not a default. (b) `entityType` tokens (`partner`, `upload`, `mls_pattern`) reach the model with no glossary; a screen-catalog-style block would stop literal narration. |
| C-80 | `entityRef` is a free-text column doing an id's job | WP-N2 review round | Writers store UUIDs, minted refs (LD-/PR-/IM-/legacy JV-), and `${profile.name} v${n}` free text in one column; every reader pattern-matches (the anchored REF_SHAPED regex is the second such guess). A typed `entity_ref_kind` column — or moving profile versions out — lets readers stop guessing. |
| C-81 | Task-form polish cluster | design audit on #134 | (a) Extract a shared `TaskForm` — Add/Edit duplicate the same field stack (F-3). (b) `Select` primitive has NO hover state (only primitive without one; two new call sites now exercise it — F-4). (c) 409 raced-completion toast fires a beat before the checkbox flips (waits on onSettled invalidate). |
| C-82 | Assignee UX follow-ons | WP-N1 build | (a) ✅ **SHIPPED as `task_assigned` (WP-NF2, PR #152)** — assigned/reassigned-to-you notification, per-user configurable, email default off. (b) My Tasks edit affordance — the form is reusable now. (c) Roster typeahead `Combobox` before any tenant approaches hundreds of seats (unpaginated Select today). |
| C-83 | Spec/standards backfill pass (C-20 convention) | this closeout | Fold into SPEC.md: TSK-12/13 (edit UI + picker), **TSK-13a** (“`work.write` implies same-stream active-seat enumeration via /api/tasks/assignees; `team.manage` gates seat MANAGEMENT, not email confidentiality” — audit-tenancy accept-and-document default), NTF-06..09, AIS-10..12 (+ the AIS-01..09 backfill still pending from PR #125). Also adopt the audits' proposed rules: PRN-08a (fan-outs address orgs by server-resolved ids returned from the command, never request bodies — implemented in #131), TST-01c (recipient-set widenings need cross-tenant + cross-org + stray-partner_id + deactivated legs), TST-11 (structural-absence sweeps must assert non-empty first — implemented in #133). |

<!-- ── N3 closeout 2026-08-19 (WP-N3A #140 · WP-N3B #141 · WP-N3C #142/#143) — candidates minted from implementation + review rounds. ── -->
| C-84 | Shared `Field` component with a built-in pending state | WP-N3A (C-51 sweep) | The portal and admin lead dialogs carry near-duplicate `Field` helpers; the portal one rendered Skeletons inside a `<span>` (the C-51 trap), the admin one dodges it via a sibling `PendingField`. One shared Field with a pending slot makes the phrasing trap unrepresentable rather than merely fixed. A lint rule banning flow-content components in phrasing parents is the belt-and-braces half (needs an ADR — new rule). |
| C-85 | Coarse-pointer 44px pass for dense controls | WP-N3B (C-52 settle) | C-52 deliberately settled at ≥24px (WCAG 2.5.8 AA) for Checkbox/FilterPill/swatches because a 44px hit area in a `gap-1.5`/`gap-2` row steals taps from the neighbor; only the Dialog ✕ got the 44px coarse-pointer tier. Going to 44px on the rest means opening container row-gaps per call site — a deliberate visual change, owner-eyeball worthy. |
| C-86 | `leads-board.tsx` hand-rolls the scroll-hint rule — adopt `ScrollHint` | PR #142 review | `leads-board.tsx:~317-324` carries its own `moreRight` + ResizeObserver copy predating the primitive; consuming `useScrollHint`/`ScrollHintFade` deletes ~12 lines and closes the third copy. Also: ~20 test files still hand-roll the ResizeObserver stub that `tests/setup.ts` now provides globally (PR #142) — a mechanical cleanup sweep. |
| C-87 | `Button` polymorphic `asChild`/`href` support | PR #142/#143 (design F-1 on #143) | Two sites now hand-roll button-shaped links (upload's in-card "Download template" `<a download>`, partner detail's "Edit partner" Link — the latter realigned to Button's recipe by hand in #143). A Radix `Slot`-based `asChild` (or `href` prop) makes the primitive the source of truth; then sweep the hand-rolled sites. Needs no new dep (radix Slot already in the tree via primitives — verify, else ADR). |
| C-88 | `/terms` discoverability + placeholder exposure | WP-N3A (C-55) | (a) Nothing links to /terms from the landing page or an app footer — the compliance story wants a footer link. (b) The page publicly serves the PLACEHOLDER `TOS_SUMMARY` and is indexable; fine per the owner's C-55 call ("visible draft beats unreadable-but-binding"), but revisit `robots`/`noindex` vs leaving it once WP-LGL-1's attorney text lands (the swap is one file: `lib/legal/tos.ts`). |
| C-89 | My Tasks overdue-elsewhere: jump-to-page (or always-render section stubs) | PR #143 review (pr-reviewer F-1) | #143 ships the honest disclosure line ("N of the overdue tasks are on another page") when the server badge outruns the page. The richer fix — clicking the badge jumps to the page holding the oldest overdue row, or group sections always render with "view page N" stubs — is a real UX design, not a patch. Compose with C-82b (task edit affordance). |
| C-90 | Portal twin of the dual lead count | PR #143 (Q3) | Q3 shipped admin-only (active badge + "N active · M total" header). The portal leads list has its own total with the same Removed-MLS invisibility question. Decide whether partners should see the distinction at all (their default view differs) before building. |
| C-91 | MyTasksList optimistic toggle leaves `groupTotals` stale one tick | PR #143 (C-60) | The complete/reopen optimistic update adjusts the row immediately but `groupTotals` (badge + headers + the new elsewhere-line) only corrects after `invalidateQueries` settles — a momentary off-by-one. Fix: mirror the toggle into `groupTotals` in the same `onMutate` (move the bucket's count between groups), rollback on error, same shape as the bell's optimistic read. |
| C-92 | Regression tests for prop-wired view gates | PR #140 review (pr-reviewer F-3) | `interactive={isDesktop}` on BOTH map call sites (dashboard + coverage) is untested prop wiring that silently regresses if someone hardcodes `interactive`. A `CoverageBody`/dashboard render test asserting the prop reflects `useIsDesktop()` closes it. Same class: MapCaption's `max-sm:hidden` (Q10). |
| C-93 | Spec/standards backfill additions from the N3 reviews (fold into C-83's pass) | N3 closeout | Add to the C-83 spec-backfill queue: **DSN-12** (every public unauthenticated standalone page renders the APP_NAME identity block — design-audit on #140), **PRN-15b** (a badge count and the list it links to must share one predicate or a test pins their equality — audit-tenancy F-3 on #143; the `tasks-group-totals` drift-guard + `leads-count` off-list test are the exemplars), **TSK-10b** (a server-computed calendar aggregate ships the date it was computed against; the client renders against that date — implemented in #143 as `MyTasksPage.today`), N3A/N3B/N3C requirement IDs, and the ENGINEERING_STANDARDS §service-role stale "11 files" parenthetical (live grep = 17). |
| C-94 | Unmatched empty-state copy: no-filter branch tells you to widen filters | WP-N3B build | `unmatched/page.tsx` filtered-empty branching shipped in #141, but the NO-filter empty still reads "Try widening the state filter or clearing the search" when nothing is set — wrong claim; should celebrate ("No unmatched leads — everything routed"). One string. |
| C-95 | Partners edit deep-link: `router.replace` RSC round trip on a `force-dynamic` shell | PR #143 (C-56) | Opening/closing the edit form syncs `?edit=` via `router.replace`, which re-runs the force-dynamic server shell each time (an RSC fetch per open/close). Tests cover state survival; eyeball perceived latency in the running app and, if it drags, switch to `window.history.replaceState` (the leads dialog's own `?open=` pattern already does exactly this — reuse it). |

<!-- ── WP-N4 closeout 2026-08-19 (PR #146 + #145 + #147) ── -->
| C-96 | Fuzzy/typo-tolerant search matching (owner option) | WP-N4 / ADR-0051 | Matching stayed EXACT substring by design; the shipped trgm indexes + `word_similarity` ranking make typo tolerance (`word_similarity(q, col) > threshold` as an OR-arm) a one-line widen. Product call: fuzzy adds false positives; decide with real-usage feedback. ADR-0051 records the option. |
| C-97 | List-ordering determinism sweep (unique final ORDER BY key) | pr-reviewer on #146 (F-2) | Ctrl-K got its `asc(refId)` final tie-break (#146 review round), but the leads list/board and other paginated lists still order by non-unique keys (`created_at` is a TRANSACTION timestamp — same-upload rows tie), so page boundaries can shuffle across identical queries. Mechanical sweep: append a stable id leg to every user-facing ORDER BY. |
| C-98 | e2e coverage gap: main-only job lets merges silently redden main | #142→#147 incident | PR CI skips e2e, so #142's AuthCardHeader change broke the main-only smoke job and FIVE merges rode over the red before it was noticed (this session's own miss — now fixed by #147). Options: (a) run the cheap unauthenticated smoke spec on PRs too (it needs no Supabase env, ~13s); (b) a branch-protection check on the main e2e job; (c) at minimum the orchestrator habit (now in memory): check `gh run list --branch main` after every merge. (a) is recommended — say the word and it's a 3-line workflow edit. |
| C-99 | N12 addendum: EXPLAIN-verify the trgm indexes at scale + decide identifier columns | audit-data on #146 | At current volume the planner ignores the new GIN indexes (cost-based; provisioned ahead of need) and `SEARCH_MIN_CHARS=2` queries seq-scan regardless (trigram needs 3 chars — documented on the constant + ADR-0051). At the N12 ~80k trigger: EXPLAIN the real multi-term shapes, confirm index uptake, and decide zip/ref_id/phone_norm's own treatment (deliberately unindexed today). Fold into the N12 row. |
| C-100 | Spec backfill add (fold into C-83/C-93 pass): **SRCH-09** fail-closed match builders | audit-tenancy on #146 (F-1 amendment) | "A search-match builder that yields no conjunct must fail CLOSED at every consumer — a scoped read never degrades to the caller's full visible set when the match predicate is absent." Implemented in #146 (guard + `isSearchable ⇒ tokenize non-empty` invariant test); wants the numbered rule so the next consumer inherits it. |

<!-- ── WP-NF2 closeout 2026-08-20 (PR #150 prefs/unsubscribe · #151 pages · #152 new types) — candidates from implementation + four-agent review rounds. ── -->
| C-101 | ✅ **SHIPPED (PR #154, merged 2026-08-20)** — every emailed link now builds from `env.APP_URL` (7 sites: reset/signup/resend + partner & team invites + digest CTAs); APP_URL normalized + path-refused at boot; whole-src conformance test (mutation-verified); Referrer-Policy no-referrer on /reset, /signup/verify, /team-invite, /unsubscribe. Residual: whether Vercel pins Host (historical-exposure question only — the dependency is gone) + preview-env APP_URL note in GO-LIVE-CHECKLIST. Original finding: | audit-security on #150 (F-3, outside-diff) | `reset/request`, `signup`, `signup/resend` build the emailed link from `new URL(request.url).origin` — a forged Host mails the victim's **password-reset token** pointed at an attacker origin (CWE-644, account takeover shape); digest CTA links (`run-upload.ts` `portalBaseUrl` ← uploads route origin) share the pattern at lower stakes. NF2's own links already do it right (`env.APP_URL`, prod-guarded). Sweep every emailed link onto `env.APP_URL` + a conformance test that no email builder takes a request-derived origin. Exploitability depends on whether the platform pins Host/x-forwarded-host — verify, don't assume. |
| C-102 | NTF-18: unsubscribe-token rotation + revocation | audit-security on #150 (F-6) | The unsubscribe token is permanent, travels in URLs, and persists in `email_outbox.html` for the retention window; a leaked link is a forever-mute for that seat. Rotate the token whenever the subject re-enables email through the authenticated NTF-15 surface; never delete overlay rows to revoke. (`Referrer-Policy: no-referrer` on `/unsubscribe` shipped in #150's fix round as the down payment.) |
| C-103 | NTF-19: RFC 8058 one-click unsubscribe headers | audit-security on #150 (F-7) | Add a `headers` leg to `enqueueEmail`/`sendEmail`; every email carrying footer links also emits `List-Unsubscribe` + `List-Unsubscribe-Post: List-Unsubscribe=One-Click` (Gmail/Yahoo bulk-sender requirements — deliverability). The endpoint must then also accept form-encoded POSTs. **Binding note:** `/api/unsubscribe` stays CSRF-exempt and Origin-free BY DESIGN for this (the csrf-conformance allowlist records it); adding an Origin check is a breaking change requiring an ADR. |
| C-104 | Partner-triggered admin fan-out coalescing (`partner_note`) — **half resolved** | audit-tenancy on #152 (F-6) + B-agent | Every partner note = one bell row (+ optional email + token mint) per admin seat, unthrottled from a partner-controlled surface; repeated failed imports each notify (deliberate, ING-08-loud — owner-flagged). ✅ **The import-failure half is RESOLVED (owner decision 2026-08-20, WP-NF2b): the per-user mute IS the control — no repeat cap.** An admin drowning in a re-uploaded broken file switches `import_result` off for themselves, and the ING-08 loudness stays for everyone who wants it. **STILL OPEN: the `partner_note` half** — an unthrottled, partner-CONTROLLED surface that fans out one row (+ optional email + token mint) per admin seat, which a per-seat mute answers only by silencing the type entirely. Candidate: per-(tenant, lead, actor) coalescing window (e.g. one row / lead / 15 min). Compose with C-75's seat-volume question — one owner decision, not three. |
| C-105 | `resolveScope` must refuse an unresolvable partner link | audit-tenancy on #152 (F-4) | A `users.partner_id` naming another tenant's (or a deleted) partner yields `partner === undefined`, the revoked-check short-circuits, and a fully live partner scope is issued anyway. Fix is R-67's twin: `if (!partner || …) throw NotProvisionedError`. Root enabler of the #152 F-1 class (the tos/accept tenant pin now defends it; prod probe 2026-08-20: 0 cross-tenant, 0 dangling links). Small WP: auth-plane change + unit legs + fixture sweep. |
| C-106 | DM-11a: composite-FK tenant backstop for subject-reference tables | audit-tenancy on #150 (F-1 durable half) | `notification_pref_overrides` (like `notifications`, `saved_views`) pairs `tenant_id` with a single-column FK to `users(id)`/`partners(id)` — nothing at the DB refuses a cross-tenant pairing (ADR-0013). Durable fix: `UNIQUE (tenant_id, id)` on the parents + composite FKs on every `tenant_id + *_user_id/*_partner_id` child. Cross-cutting migration; the app-layer guard (`assertSubjectInTenant`, #150 fix round) holds the line meanwhile. Draft rule: subject-reference tables declare the pairing at the database or carry an ADR saying why not. |
| C-107 | Proxy page-prefix conformance test | audit-tenancy on #151 (F-4) | `PROTECTED_PAGE_PREFIXES` is a hand-maintained literal list that has needed a bespoke entry two WPs running (`/tasks`, `/notifications`). A unit test enumerating `src/app/(admin)/*/page.tsx` and asserting `isProtectedPage("/"+dir)` (explicit allowlist for `/terms`-style public pages) makes the next forgotten entry a red test instead of a signed-out shell render. |
| C-108 | ✅ **CLOSED — deliberate exemption (owner decision 2026-08-20, WP-NF2b).** LGL-01 gate on the notification feed | audit-tenancy on #151 (F-3) | `/api/notifications` (and now the two `/notifications` pages) carry no ToS gate while the sibling `/api/me/notification-prefs` added one — a seat required to re-accept a bumped ToS can still read notification titles/deep links (lead refs + status words; never seller PII). **Resolution: no ToS gate, recorded in the route header.** The LGL-01 acceptance boundary is PAGE-level (a re-accept-gated seat is held at the acceptance screen), and the feed carries lead refs + status words + filenames + fixed generic sentences by construction (NTF-16) — never seller PII. Gating it would also break the bell in the chrome of the acceptance screen itself. The header states the condition that kills the exemption: a feed that ever carries lead CONTENT rather than refs. Write-side surfaces (incl. `PUT /api/me/notification-prefs`) keep their gate. |
| C-109 | AUTHZ-04 ban extension: role literals in query predicates | audit-tenancy on #152 (F-5) | The role-literal ban only matches JS comparisons; `eq(users.role, "admin")` — the SAME decision as a predicate — passes unseen (`activeAdminSeats`, `activePartnerSeats`). Extend the regex to `eq(<table>.role, "<literal>")` outside `lib/scope.ts`/`lib/authz.ts` + a reasoned allowlist (it WILL light up existing sites; `activeAdminSeats` now carries the ⚠️ SCOPE-GUARD-ADJACENT marker from #152's fix round). |
| C-110 | TST-01d sweep: make the tenant leg load-bearing in every ownerWhere suite | audit-tenancy on #151 (F-1) | The #151 lesson, generalized: a fixture whose subjects differ in BOTH ownerWhere legs proves only the user leg — the feed suite needed a deliberate same-user-OTHER-tenant row before `eq(tenant_id,…)` was falsifiable (mutation-verified in #151's fix round). Sweep the other per-user tables (`saved_views` first) for the same one-row probe. Draft rule TST-01d: an isolation test whose failure depends on planner order, insertion order, or a UUID tie-break is not an isolation test. |
| C-111 | e2e smoke for the paginated feed | C-agent on #151 | No e2e exercises `?cursor`/`?limit`/Load-more; a one-step "load more" leg in the smoke spec would catch route regressions the unit tier can't (composes with C-98's run-smoke-on-PRs option). |
| C-112 | Return the raw overlay from `/api/me/notification-prefs` GET | C-agent on #151 | The endpoint returns masked EFFECTIVE values, so a per-event email choice pinned before "Pause all emails" is unrecoverable on unpause (the UI disables the column meanwhile — aria-disabled). Returning the raw overlay alongside `overridden` lets the prefs card restore the pre-pause state. |
| C-113 | Feed read-path economies | C-agent + audit-tenancy perf notes on #151 | (a) `unreadCount` recomputes on every Load-more page — a `?count=0` opt-out drops one round trip per page. (b) Focus-refetch was capped to first-page-only in #151's fix round (TanStack predicate form) — revisit if deeper-page staleness ever matters. |
| C-114 | Spec backfill additions from the NF2 reviews (fold into the C-83/C-93/C-100 pass) | NF2 closeout | **NTF-10..NTF-17** (WP-NF2's requirement IDs) into SPEC.md; **TST-01d** (C-110's rule); **API-01a** ("a route that never calls getServerScope is a PUBLIC route and is listed as one in a conformance allowlist naming its standing mechanism" — the csrf-conformance test implements the CSRF half); **DM-11a** (C-106's rule); AUT note that `/api/unsubscribe` carries `UNSUBSCRIBE_THROTTLE` + `withUniformTiming` (the AUT-03/05 family grew a member); and the #152 rule that concurrent `onConflictDoNothing()` get-or-create patterns backing capabilities ship a concurrency test in the same PR. Add the **NF2b** amendment too: prefs resolve `defaults ⊕ subject overlay` with NO workspace layer, and `/api/settings/notifications` no longer exists. |

<!-- ── WP-NF2b 2026-08-20 (per-user-only prefs; the workspace matrix retired) ── -->
| C-115 | A partner ORG that unsubscribed its shared mailbox cannot re-subscribe itself | WP-NF2b | Org-addressed email (`partner_digest`, partner hot alerts → `partners.email`) is governed by the shipped default ⊕ the **partner-ORG** overlay row, and the ONLY writer of that row is the tokenized unsubscribe link in the mail. An org is not a seat, so it has no `/api/me/notification-prefs` — once someone clicks "stop all", turning it back on is a support/SQL action with no UI anywhere. Acceptable today (the click is deliberate and rare, and every partner SEAT still gets its own bell rows), but it is a one-way door on a shared mailbox. Candidate: surface the org's email state on the admin partner-detail page as a read + a re-enable action, audited — deliberately NOT a silent admin re-subscribe. Compose with C-104. |
| C-116 | Signed-out partners land on the ADMIN login screen from any non-`/portal` link | WP-NF2b | `src/proxy.ts` picks the login screen by path PREFIX (`/portal*` → `/portal/login` OTP, everything else → `/login` password). So the unsubscribe page's new "Manage all notification preferences" → `/notifications` link (and any other stream-agnostic link we ever mail) is a dead end for a signed-out partner, who has no password. Signed IN it merely degrades: the `(admin)` layout bounces them to `/portal/dashboard`, one click from their own page. Candidate: a tiny stream-aware entry route (`/go/notifications`, or a `/login` that offers the OTP screen when the address it is given is a partner seat) — the same shape would fix every future cross-stream deep link. Not a security issue; both screens refuse the wrong credential. **CONFIRMED by audit-tenancy F-5 on #155** (a partner arriving from the unsubscribe link lands on the admin login), whose suggested fix is a neutral **`/preferences` stream-router**: one public path that resolves the caller's stream and forwards to `/settings/notifications` or `/portal/notifications`, with the un-authenticated case choosing the right login screen. Prefer that shape — it is the thing every mailed link should point at, not just this one. |
| C-117 | **WP-NF2c — an off switch for the env ops mailbox** (`ADMIN_ALLOWLIST`) | WP-NF2b (ADR-0053 consequence) | The allowlist addresses in `resolveAdminEmails` (run-upload.ts) belong to no seat, so they have no overlay to gate them and no unsubscribe footer (WP-NF2 §10.3, deferred item 3) — and now, with the workspace matrix retired, **no switch at all**. They receive every admin run-summary and hot alert unconditionally, and nobody in the app can stop them. Two options, owner's call: (a) an env off-switch / per-event allowlist so the ops mailbox is configurable where it is configured; or (b) **drop the allowlist from `resolveAdminEmails` entirely** — `activeAdminSeats` already reaches every real admin seat with per-seat prefs and unsubscribe links, so the allowlist may simply be pre-Phase-C scaffolding that outlived its purpose. (b) is smaller and removes a whole ungoverned email path; (a) keeps a deliberate ops-mailbox capability. Check whether the value is even set in prod before choosing. |

<!-- ── WP-N5 2026-08-20 (lead record side panel + inline editing; PRs #157–#161) ── -->
| C-118 | Dedupe-collision 409 mapping in `editLead` | WP-N5 PR B | N5-12 assumed an address/ZIP edit collision returns 409, but `editLead` lets the `(tenant, dedupe_key)` unique violation escape and the route's catch-all returns a 500 with the static "Edit failed." (C-17). Map the unique-violation error to a 409 with a human message ("Another lead already has this address"); the client half is already built + tested against a stubbed 409. |
| C-119 | "Changes" timeline filter chip | WP-N5 PR B | `details_updated` entries show under All only — the three chips are streams and Status would make the C-12 empty copy lie. If edit-history reading becomes a real workflow, a fourth "Changes" chip is the shape. |
| C-120 | Portal feed showing "Details updated" | WP-N5 PR B | Owner call. `audit_log` has NO partner scope builder (the derivation now throws on partner streams — refuse-rather-than-widen), so portal visibility needs a deliberate design (probably a separate partner-safe source), not a gate flip. |
| C-121 | Retry toast action for StatusSelect + task completion | WP-N5 PR B | The inline-edit failure toast got an action slot (`Toast.action`) + scoped dismissal; the sibling mutations (status change, task complete/reopen) still fail with a plain message. Same recipe applies. |
| C-122 | `Select.renderValue` → accessible-name mechanism | audit-a11y on #160 | The inline fix composes the trigger's `aria-label` with the current value at the call site. The durable fix lives in the primitive: mirror `renderValue` content into the accessible name (sr-only value node / `aria-labelledby`) so every future `renderValue` caller is correct by construction. |
| C-123 | Admin e2e auth harness | WP-N5 PR B fix round | There is NO admin e2e auth path; `tests/e2e/admin-inline-edit.spec.ts` (counts PATCHes on an Enter commit — the jsdom-invisible double-fire class) self-skips on `E2E_AUTH_READY`, same as `portal-journey`. Building the harness un-skips both and unblocks TST-07. Composes with the owner reality-gate item. |
| C-124 | Action toasts unreachable inside the <768px modal sheet | audit-a11y on #160 (F-7) | `ToastProvider` mounts outside the Radix focus trap, so at sheet widths a keyboard user cannot Tab to a toast's Retry. Admin documents ≥768 so it's Low today — but the portal lives there. Fix is ToastProvider portal wiring (render the actionable stack inside the trap boundary), broader than one WP. The new §9 degrade-don't-trap rule (shipped in #160) names the class. |
| C-125 | Uniqueness index on `leads (tenant_id, ref_id)` | audit-tenancy on #160 (F-5) | `entity_ref` addressing and `getAdminLeadDetail`'s `[lead] =` both assume ref uniqueness that no constraint enforces (partners/uploads have theirs). Needs a prod duplicate check before the migration. |
| C-126 | Admin `?open=` seed shape-validation twin | audit-tenancy on #159 (F-2) | #159 shape-checks the portal `?open=` seed and `encodeURIComponent`s the fetch path segment; the admin leads page has the identical pattern unhardened (same defence-in-depth argument — the mis-target is capability-gated today). The new FRONTEND_STANDARDS §1 rule names the class. |
| C-127 | Esc-discoverability hint on the side panel | audit-a11y on #158 (F-5) | No WCAG breach; `GlobalSearch` shows "esc" in its footer, the panel shows nothing. A small `text-text-3` hint near ✕ if the owner wants the consistency. |
| C-128 | ADR for a real `FocusScope` dependency | #158 fix round | `@radix-ui/react-focus-scope` is not a declared dep and not re-exported; `DiscardGuard` ships zero-dep containment (Tab cycle + `inert`) instead. If a second trapped-overlay need appears, an ADR + package.json entry beats a second hand-rolled trap. |
| C-129 | `Dialog.tsx` dead `aria-label` branch | #158 fix round | Same `typeof title === "string"` bug fixed on SidePanel (#158 finding 6) exists verbatim in Dialog. One line whenever Dialog is next opened. |
| C-130 | Promote `ArrowButton` on its third consumer | WP-N5 PR A | `ARROW_BUTTON_CLASS` (now with active + coarse-reach states) is shared by `Pagination` and the lead pager as an exported class string; a third user should make it a real component. |
| C-131 | Portal list pager (portal C-59 twin) | WP-N5 PR C | The portal lead panel deliberately shipped without prev/next N-of-M (N5-20); the portal list owns its working set the same way the admin list does, so the same recipe applies when wanted. |
| C-132 | Worktree-safe full-suite test script | WP-N5 fix rounds (bit twice) | Bare `pnpm vitest` in a worktree with `.env.local` fans all ~103 integration files at the 15-client pooler in parallel — phantom reds (16 files "failed") that CI never sees because its jobs are split. A `test:all` script that runs `test:unit` then `test:integration --no-file-parallelism` makes "run everything" safe to say to an agent. |
| C-133 | `audit_log` growth: retention/pruning + batched derivation | WP-N5 PR D | The table is append-only and effectively never pruned; 0059's covering index fixes the read, not the growth. Compose with the standing audit-PII-gap candidate (purge hatch exists). Separately: if a future feed derives `details_updated` across MANY leads at once, batch the per-lead query. |

<!-- ── WP-N5e 2026-08-21 (owner hands-on polish round; PR #164) ── -->
| C-134 | `Select` trigger has NO hover/pressed state app-wide | audit-design-system on #164 | `RECORD_CONTROL_CLASS` patches hover/active for the two record controls only; every other `Select` in the app still has neither (DSN-03 gap across the whole primitive). Fix in `Select.tsx`'s base so all call sites inherit it. |
| C-135 | `PartnerTag` truncates the partner name behind a `title` attribute | audit-design-system on #164 | `title` is invisible to touch and to some AT (PRN-14 adjacency) — the one remaining `truncate` inside the record grid. Wrap-or-popover instead. |
| C-136 | Address-group leg for the admin e2e PATCH-counting spec | WP-N5e | `tests/e2e/admin-inline-edit.spec.ts` (self-skipping until C-123's auth harness) exercises only top-level fields; the four address columns carry the same real-browser double-blur risk. One more leg when the harness lands. |
| C-137 | Promote the record-field vocabulary (`FieldLabel`/`Field`/`ControlField`) to `src/components` | #164 fix round | The polish round's two Medium drifts (portal `Field` losing the WP-UX-7 "Not provided" demotion; `StatusSelect`'s field variant losing `justify-between` + the 120ms border transition) were BOTH hand-copy drift of this vocabulary. The fix round patched both copies + added drift tests; a promoted `RecordField.tsx` (+ gallery card) stops the class recurring. FRONTEND_STANDARDS §2 promotion rule. |
| C-138 | Close the `escapeHeld` effect-hop window | pr-reviewer on #164 (F-2, low-confidence) | An Escape in the sub-frame between opening an inline field and the effect flush can reach Radix's capture listener as "close panel" (pre-existing for every InlineField; the address group adds one hop). Fix is a synchronously-written ref driving `escapeHeld` for the whole family — shared-primitive change, real-browser stress test to confirm severity first. |

**Proposed house rule (WP-TSK-4, pr-reviewer F-5):** a dismissible inline form (add/edit panels that expand in place, not a modal) returns focus to its own trigger control on both cancel AND success — the trigger is the last thing the keyboard/screen-reader user had focus on before the form appeared, so it's where focus should land once the form goes away. Candidate for promotion into `docs/FRONTEND_STANDARDS.md` §6.17 alongside the existing state-matrix rule. (Applied again in WP-N1's edit form, PR #134 — promotion into the standards doc still pending.)

**Proposed house rule (audit-tenancy on C-11):** a predicate that decides *which users belong to the caller's stream/org* is a scope builder and lives in `src/lib/scope.ts`; module-local copies require the `⚠️ SCOPE-GUARD-ADJACENT` marker and a named cross-reference to every sibling definition. (→ C-47 — ✅ ADOPTED: landed in ENGINEERING_STANDARDS §2 via PR #134.)
