# WP-008: MLS filter engine
Spec: MLS-01..05, PRN-04, PRN-01 · Phase: 0 · Depends: — · Tier: A

## Goal
A pure, deterministic pattern engine over Notes: disqualify / keep-override /
blank-keep semantics, anchored patterns only, with the matched span retained for
highlighted display.

## Definition of done
- [x] `src/modules/pipeline/mls.ts` — pure `evaluate(notes, patterns) → {verdict, reason, pattern?, match?}`; no I/O (PRN-01); stateless regex (no g/y flags)
- [x] Keep-override beats disqualify (MLS-02); blank/missing/no-match ⇒ keep (MLS-03)
- [x] Anchored patterns only; negatives tied to the listing question (PRN-04)
- [x] Matched pattern + text span returned for removed leads (MLS-05)
- [x] `src/modules/pipeline/mls-patterns.ts` — seed patterns (MLS-01/02) as the mls_patterns table seed
- [x] `isValidPatternRegex` for the admin pattern editor (MLS-04)
- [x] TST-02 corpus incl. the four canonical cases + PRN-04 false-positive guards; 30 tests green

## Verification (run)
- `pnpm exec vitest run tests/unit/mls.test.ts` → 30 tests green
- `pnpm run check` → whole suite + typecheck + lint green

## Out of scope (→ later WPs)
- mls_patterns DB table + admin editor UI (WP-005 schema; Phase 2 UI) — engine already takes patterns as data
- Wiring evaluate() into the pipeline run + persisting mls_status/mls_reason (Phase 1 spine)

## Notes / observed candidates (do not build now)
- Real notes may use "on the market" (with 'the'); spec lists only "on market".
  If seen in real data, add the case to the corpus first, then a pattern via the
  admin table — never a preemptive guess (anti-drift).
