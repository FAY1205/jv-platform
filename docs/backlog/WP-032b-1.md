# WP-032b-1: File templates + recognized-format list (ING-05, SET-12 view)
Spec: ING-05, SET-12 (view) · Phase: 2 · Tier: B · Depends: WP-032a (Rules area), sources module
## Goal
Download an .xlsx template (expected headers + one example row) rendered from a
Source Profile (ING-05), and see the recognized upload formats on the Rules screen
(SET-12, read-only). The format-drift diff-and-confirm flow (ING-08) + editable/
versioned Source Profiles are the sibling Tier-A WP-032b-2.
## Definition of done
- [x] `src/modules/sources/template.ts` — pure `templateRows(profile)` (headers = signature; example fills mapped canonical columns) + `renderTemplate` (.xlsx, cells written as explicit strings)
- [x] `GET /api/templates/[id]` (admin) — downloads the template for a known format (generic / investorfuse); 404 for unknown
- [x] `GET /api/admin/rules` also returns `formats` (from SEED_SOURCE_PROFILES — what detection actually uses)
- [x] `/rules` "File formats" section (name, version, columns, strict/flexible + Download); Upload page "Download template" link (ING-05: expected format shown on the upload surface)
- [x] Live-verified: 3 unit tests (template rows); app E2E (formats list; generic + investorfuse templates download as valid .xlsx; unknown → 404)
- [x] Fixed a latent WP-028b test bug: `export-storage.test.ts` called `createClient` in the describe body (evaluated even for `.skip`), throwing at collection without Supabase env — moved to `beforeAll` (skips cleanly now)
## Out of scope (→ WP-032b-2)
Format-drift diff-and-confirm on upload — partial signature match → column diff → proposed mapping → confirm → new profile version (ING-08). Editable/versioned Source Profiles + per-profile strictness toggle (SET-12 edit). Storing profiles in the DB (currently code seeds drive detection).
## Tests
- tests/unit/template.test.ts — templateRows headers/example alignment (ING-05)
