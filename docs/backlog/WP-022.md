# WP-022: TST-05 golden gate (semantic zero-diff)
Spec: TST-05, DM-08, §11 Phase-1 exit · Phase: 1 · Tier: A + phase gate

## Goal
Lock the pipeline's output over the real (anonymized) week so any future drift fails CI —
the "institutional memory" that keeps determinism honest as rules/code evolve.

## Definition of done
- [x] `scripts/gen-golden.ts` — runs the PURE pipeline (`planRun`) over the anonymized week
      under a FIXED rule set + pinned coverage, emits `tests/fixtures/investorfuse-week-golden.json`
      ({ rulesHash, outcomes[] }). Regenerate deliberately when a rule change is confirmed.
- [x] `tests/unit/golden.test.ts` (TST-05): recomputes and asserts a **semantic zero-diff** on the
      decision fields (MLS verdict · assignment method · partner · campaign code · previously-matched),
      plus the pinned rules-snapshot hash (DM-08). Confirms the mix (kept/removed, zip/state/unmatched).
- [x] Golden distribution: 50 leads → 26 removed / 24 kept · 2 ZIP-override / 47 state / 1 unmatched.

## Phase-1 exit gate — OWNER-gated (§11)
The harness is built + green. The reality gate itself is the owner's:
1. **Hand-verify the golden** against your manual output for that week (the current golden is
   *pipeline-generated* — a provisional baseline; reconcile any real differences, then regenerate).
2. **Process one real week end-to-end** in the app (`/upload` → results → download).
3. **Live integration** — run the 3 DB suites once `DATABASE_URL` points at the IPv4 **pooler**
   (`Session pooler` in the Supabase dashboard; the direct `db.<ref>.supabase.co` host is now IPv6-only):
   `node --env-file=.env.local ./node_modules/vitest/vitest.mjs run tests/integration`.

## Verification
typecheck + lint + 184 unit tests (incl. 3 golden) + build green.
