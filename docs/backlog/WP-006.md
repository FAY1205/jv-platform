# WP-006: Scoping guard + isolation suite (TST-01)
Spec: PRN-08, PRN-13, SEC-01, TST-01 · Phase: 0 · Depends: WP-005 · Tier: A

## Goal
The app-layer scoping guard every query passes through, and a live isolation suite
that proves no query path crosses a tenant or partner boundary.

## Definition of done
- [x] `src/lib/scope.ts` — ScopeContext + WHERE builders: `tenantWhere`, `leadWhere` (partner scope), `noteWhere` (PRN-13 note visibility), `leadChildWhere`; `requirePartner` guard against unscoped partner queries
- [x] TST-01 integration suite (`tests/integration/isolation.test.ts`): two tenants + two partners; proves tenant isolation, partner isolation, admin-sees-all-tenant, and PRN-13 admin/partner note visibility both directions; asserts RLS enabled on ≥20 tables
- [x] Self-skips when DATABASE_URL is unset (keeps the fast unit suite green)
- [x] CI runs integration tests on every merge against an ephemeral Postgres service (never the cloud project)

## Verification (run — passed)
- Applied migrations + seed to the dev Supabase project (EU)
- `pnpm test:integration` against the live dev DB → 6/6 green
- `pnpm run check` → typecheck + lint + 80 unit tests green; integration self-skips without DB

## Out of scope (→ later WPs)
- RLS enforced as the `authenticated` role with JWT claims + GRANTs — WP-007 (auth wires the claims); the app-layer guard is the primary enforcement for the admin/service path
- Applying the scope guard across real API route handlers — Phase 1/2 as routes are built
