# WP-023: Supabase Auth + real getServerScope + admin login
Spec: AUT-01/02/05/09/12/13, ACC-01 (admin) · Phase: 2 · Tier: A · Depends: WP-006 (scope guard), WP-007 (auth primitives), schema `users`
## Goal
Replace the Phase-1 dev-scope stub with real Supabase Auth. Admin signs in with
email+password; `getServerScope` resolves tenant/role/partner from the verified
session via the authoritative `users` row. Secure cookies, CSRF, uniform login,
no-store on authed pages. Load-bearing for the whole portal (WP-025+).
## Definition of done
- [x] `@supabase/ssr` + `@supabase/supabase-js` added (ADR-0009)
- [x] `getServerScope` resolves from the verified auth uid → `users` row (admin + partner ready); pure `resolveScope` unit-tested
- [x] `proxy.ts` (Next 16): session refresh + protected-page redirect + `no-store` (AUT-13)
- [x] `POST /api/auth/login` — uniform failure + floored timing (AUT-05), Origin/CSRF (AUT-12), single attempt (no enumeration)
- [x] `POST /api/auth/logout` — server-side Supabase sign-out (basic; full ACC-02 in WP-024)
- [x] `POST /api/auth/change-password` — re-auth + strength + breach gate (AUT-02); `/account/password` screen
- [x] `/login` admin screen (component library, tokens)
- [x] Session cookie `__Host-jv-auth`: HttpOnly, Secure, SameSite=Lax, Path=/ (AUT-12)
- [x] 5 existing API routes return uniform 401/403 on auth failure
- [x] `scripts/provision-admin.ts` (dev): create auth user + app_metadata + `users` row
- [x] Live-verified end-to-end against the dev project (sign-in → scope → authed data)
## Out of scope (→ later WPs)
Rate-limit (AUT-03), lockout+notify (AUT-04), password reset/forgot (AUT-06),
session-fixation refresh-on-privilege-change (AUT-07), "sign out all devices" +
sessions registry (AUT-14/ACC-02) → **WP-024**. Partner email-OTP onboarding
(PTL-01) → **WP-025**. TOTP MFA (AUT-08) → later. Live zxcvbn meter (dynamic import)
on the change-password screen → candidate.
## Tests
- tests/unit/scope-context.test.ts — TST-12: resolveScope mapping + auth errors
- tests/unit/auth-csrf.test.ts — AUT-12/09: Origin allowlist + constant-time token
- tests/unit/auth-login.test.ts — AUT-05: uniform failure policy
- tests/unit/auth-password-gate.test.ts — AUT-02: strength → breach ordering
- tests/integration/auth.test.ts — TST-12 (live, self-skip): real sign-in → scope; wrong-pw rejected
