# WP-032a: Rules area — recodes + MLS on/off + coverage view (CVG-02, DM-08, PRN-04)
Spec: CVG-02, DM-08, PRN-04 · Phase: 2 · Tier: A · Depends: WP-023 (auth), Phase-0 rules tables/seeds
## Goal
A unified admin **Rules** screen: full CRUD on campaign recodes; view + on/off toggle
+ label edit for MLS phrases (regex NEVER editable at runtime, PRN-04); a read-only
coverage summary linking to Partners. Every change audited; the next run captures the
live rules into its immutable snapshot (DM-08). Source Profiles + drift screen → WP-032b.
## Definition of done
- [x] `src/modules/rules/schema.ts` — `RecodeSchema` (trim/require), `MlsPatternUpdateSchema` (label + enabled only; strips any smuggled `regex` — PRN-04)
- [x] `src/modules/rules/queries.ts` — `listRecodes`, `listMlsPatterns`, `coverageSummary` (zip count + state rules w/ partner), scoped
- [x] `src/modules/rules/commands.ts` — recode create/update/delete; `updateMlsPattern` (label/enabled only); all audited (`recode.*`, `mls_pattern.updated`)
- [x] Routes (admin + CSRF): `GET /api/admin/rules`; `POST /api/admin/rules/recodes`, `PATCH/DELETE .../recodes/[id]`; `PATCH /api/admin/rules/mls/[id]`
- [x] `/rules` page — recodes table + modal CRUD, MLS phrases table + on/off toggle, read-only coverage summary; TopBar "Rules" nav; `/rules` proxy-protected
- [x] Live-verified: 3 integration tests (recode CRUD + audit; MLS toggle changes enabled/label but not regex; coverage summary); app E2E (12 MLS + 2 recodes load; create/toggle/delete; regex-smuggle stripped)
## Out of scope (→ WP-032b / later)
Source Profiles view/edit/version + per-source & generic template download (SET-12, ING-05) → **WP-032b**. Format-drift diff-and-confirm on upload (ING-08) → **WP-032b**. State-rule editing here (edited per-partner on Partners, 031a) — shown read-only. Adding NEW MLS patterns / editing regex at runtime → intentionally NOT built (PRN-04: MLS token changes are dev-time, corpus-first).
## Tests
- tests/unit/rules-schema.test.ts — recode + MLS-update validation (CVG-02, PRN-04)
- tests/integration/rules.test.ts — recode CRUD + audit, MLS toggle (regex untouched), coverage summary (live, self-skips)
