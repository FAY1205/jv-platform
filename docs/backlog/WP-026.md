# WP-026: Scoped partner portal views (leads · statuses · export)
Spec: PTL-02/03/04, SEAM-06, TST-08 · Phase: 2 · Tier: B · Depends: WP-025a/b, WP-006 (scope guard)
## Goal
The partner sees ONLY their own leads (leadWhere), can update a lead's status
(→ lead_status_history + event, visible to admin), and can export their own leads.
Notes (two-stream) are WP-027.
## Definition of done
- [x] `src/modules/portal/statuses.ts` — SEAM-06 seed vocabulary + `isValidStatus` + `currentStatus` (unit-tested)
- [x] `src/modules/portal/queries.ts` — scoped `listPartnerLeads` (paginated, FEP-03), `getPartnerLeadDetail` (+ status history via leadChildWhere), `getPartnerExportData`
- [x] `src/modules/portal/status-update.ts` — `updateLeadStatus` (txn: scoped verify → history + event; PRN-05 assignment untouched)
- [x] `GET /api/portal/leads` (paginated) · `GET /api/portal/leads/[ref]` · `POST .../status` (CSRF) · `GET .../export` (.xlsx, SEC-06)
- [x] `/portal/leads` (list, TanStack Query, pagination, export) · `/portal/leads/[ref]` (detail + status dropdown + history) · `/portal` nav
- [x] Proxy: `/portal/*` unauth → `/portal/login`; admin pages → `/login`
- [x] Live-verified: TST-08 scoping (partner sees only own, updates only own, admin sees change); unauthed routes → 401/redirect
## Out of scope (→ later WPs)
Two-stream notes (NTS, PRN-13) → **WP-027**. Portal mini-stats / status funnel (PTL-05, ANA-05) → WP-029/Phase-3. Listing check LinkOnly (LST) → WP-033. List virtualization (@tanstack/react-virtual) if a page grows large → follow-up (server pagination bounds the DOM at 50/page for now). Editable status list (SET-04) → WP-032.
## Tests
- tests/unit/portal-statuses.test.ts — SEAM-06 vocabulary + current-status resolver
- tests/integration/portal-scope.test.ts — TST-08 (live): partner isolation + scoped status update + admin visibility
