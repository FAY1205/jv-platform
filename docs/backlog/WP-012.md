# WP-012: Synthetic fixture harness
Spec: §12.1, TST-05 (provisional) · Phase: 0 · Tier: B

## Goal
Realistic synthetic multi-source lead files that drive the whole pure pipeline,
establishing the golden harness for Phase 1 until real files arrive.

## Definition of done
- [x] `tests/fixtures/synthetic-week.ts` — two source formats (GENERIC + a second "Bees" profile with renamed headers), 14 leads with per-lead expected outcomes
- [x] Exercises leading-zero ZIPs (CT/NJ), full-name vs coded states, and the MLS positive/negative/blank cases
- [x] `tests/unit/pipeline-fixtures.test.ts` — drives detect → apply → normalize → MLS over both sources and asserts every expected outcome (the provisional TST-05 golden)

## Verification (run)
- `pnpm run check` → 100 unit tests + typecheck + lint green; `pnpm run build` → compiles

## Out of scope (→ Phase 1)
- Assignment + dedupe outcomes in the golden (added when the spine exists); pin to a rules snapshot then (TST-05 semantic zero-diff)
- Replacing synthetic data with 2+ real files per source + one hand-verified week (owner critical-path item)
- Reading real .xlsx/.csv (SheetJS in a Web Worker) — the fixtures are already-parsed rows
