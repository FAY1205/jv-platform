# WP-004: Component library core
Spec: DSN-03..10 · Phase: 0 · Depends: WP-003 · Tier: B · Status: DONE

## Goal
The one component library (nothing UI ships outside src/components), each
interactive component defining all states (default/hover/focus-visible/active/
disabled/loading), consuming tokens only.

## Definition of done
Preview slice (done — for design sign-off):
- [x] `cn` helper (no dependency)
- [x] Button (primary/secondary/ghost/danger · sm/md · loading/disabled)
- [x] Badge (zip/state/removed/warn/prev/success/neutral/outline · optional dot)
- [x] Card / CardHeader / CardTitle / CardBody
- [x] Stat (KPI readout — display-face tabular numerals)
- [x] PartnerTag (signature: color + name + mono reference ID — PRN-14)
- [x] Input (label, hint, error; a11y-bound — FRM-01/04)
- [x] Skeleton
- [x] `/gallery` preview route (light/dark)

Remaining (after design sign-off) — DONE:
- [x] Select, Table primitive (sticky headers, sortable), Tabs, Modal, Toast, Tooltip, Empty state
- [x] Component tests (jsdom + Testing Library) asserting rendered states — 9 tests
- [x] Gallery uses the Table primitive; showcases every component in light/dark

## Verification (run)
- `pnpm run typecheck` / `lint` → 0; `pnpm run test` → 25 tests green; `pnpm run build` → compiles
- Rendered `/gallery` in-browser (light + dark) during the design sign-off

## Notes
Design thesis: "routing ledger / control room." Distinctive moves = the PartnerTag
signature, ledger-grade tabular numerics, and the partner-colored routing table.
Baseline stays faithful to the demo's language.
