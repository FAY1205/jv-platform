# WP-009: Normalization module
Spec: NRM-01/02, DM-01 · Phase: 0 · Depends: — · Tier: B

## Goal
Pure canonical-form functions for ZIP, phone, state, and address, plus the
dedupe key, so leading-zero ZIPs survive and equivalent leads collapse.

## Definition of done
- [x] `normalizeZip` — first digit group, first 5, left-pad (`6404`→`06404`, `06404-1234`→`06404`); both lead ZIPs and coverage import
- [x] `normalizePhone` — digits-only, last 10
- [x] `normalizeState` — 2-letter code; full names accepted (50 + DC + PR)
- [x] `normalizeAddress` — lowercase, punctuation→space, whitespace collapsed (display preserved elsewhere)
- [x] `computeDedupeKey` — normalized(address)+zip5 (DM-01)
- [x] Tests: NRM-01 padding + TST-03 `6404`↔`06404-1234`; NRM-02 phone/state; TST-04 dedupe-key equivalence

## Verification (run)
- `pnpm exec vitest run tests/unit/normalize.test.ts` → 11 tests green

## Out of scope (→ later WPs)
- Wiring into the pipeline run + writing dedupe_key/normalized fields to leads (Phase 1 spine)
- Phone as secondary dedupe confirm key (DED-01, Phase 1)
