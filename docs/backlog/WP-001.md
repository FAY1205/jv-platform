# WP-001: Repo + CI scaffold
Spec: Playbook §2/§3, FEP-08 · Phase: 0 · Depends: — · Tier: B

## Goal
A runnable Next.js/TypeScript app on the Playbook §2 layout with the toolchain,
test runners, CI, and binding docs in place — the foundation every later WP builds on.

## Definition of done
- [x] Next.js App Router + TS app scaffolded (`src/` dir, `@/*` alias)
- [x] Playbook §2 module structure created (`src/modules/*`, `src/lib/*`, `src/db`, `src/workers`, route groups)
- [x] `CLAUDE.md` placed verbatim (Playbook §3); `docs/SPEC.md` + `docs/PLAYBOOK.md` + demo reference in repo
- [x] Scripts: dev, build, start, lint, typecheck, test, test:unit, test:integration, e2e, check
- [x] Vitest wired (unit smoke test green) + Playwright wired (e2e smoke spec, runs in CI on main)
- [x] GitHub Actions CI: typecheck + lint + tests on every PR; e2e on main (Lighthouse stubbed until real routes)
- [x] `.env.example` with SEC-07 env-separation guardrails documented
- [x] git initialized; `.gitignore` commits `.env.example`, ignores e2e artifacts
- [x] ADR-0001 records framework versions + Tailwind adoption

## Verification (run)
- `pnpm run typecheck` → 0
- `pnpm run lint` → 0
- `pnpm run test` → 1 file / 1 test passed
- `pnpm run build` → compiled successfully
- e2e: wired, not executed locally (Playwright browsers not installed; CI-main only)

## Out of scope (→ later WPs)
- Design tokens + Tailwind theme wiring (WP-003)
- Component library (WP-004)
- Security headers / CSP (WP-007, SEC-04)
- Lighthouse CI config against real dashboard/portal routes (WP-003/004+)
- Testing Library / jsdom for component tests (added in WP-004)

## Notes
- Node 22.20 / pnpm 11.9 / git 2.51 present. Docker + Supabase CLI NOT installed —
  required before WP-005 (schema/RLS). Flagged in README owner checklist.
